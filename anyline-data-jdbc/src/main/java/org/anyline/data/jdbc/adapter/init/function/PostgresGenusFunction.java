/*
 * Copyright 2006-2025 www.anyline.org
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

package org.anyline.data.jdbc.adapter.init.function;

import org.anyline.metadata.SystemFunction;
import org.anyline.metadata.type.DatabaseType;
import org.anyline.util.BeanUtil;

import java.util.List;

public enum PostgresGenusFunction implements SystemFunction {
    ABS(META.ABS, "ABS"),
    COUNT_NOT_NULL(META.COUNT_NOT_NULL, "NUM_NONNULLS "),
    COUNT_NULL(META.COUNT_NULL, "NUM_NULLS"),
    CBRT (META.CBRT , "CBRT"),
    CEIL(META.CEIL, "CEIL"),
    CEILING(META.CEILING , "CEILING"),
    DEGREES(META.DEGREES , "DEGREES"),
    DIV(META.DIV, "DIV"),
    ERF(META.ERF, "ERF"),
    ERFC(META.ERFC, "ERFC"),
    EXP(META.EXP, "EXP"),
    FACTORIAL(META.FACTORIAL, "FACTORIAL"),
    FLOOR(META.FLOOR, "FLOOR"),
    GAMMA(META.GAMMA, "GAMMA"),
    GCD(META.GCD, "GCD"),
    LCM(META.LCM, "LCM"),
    LGAMMA(META.LGAMMA, "LGAMMA"),
    LN(META.LN, "LN"),
    LOG(META.LOG, "LOG"),
    LOG10(META.LOG10, "LOG10"),
    MIN_SCALE(META.MIN_SCALE, "MIN_SCALE"),
    MOD(META.MOD, "MOD"),
    PI(META.PI, "PI"),
    POWER(META.POWER, "POWER"),
    RADIANS(META.RADIANS, "RADIANS"),
    ROUND(META.ROUND, "ROUND"),
    SCALE(META.SCALE, "SCALE"),
    SIGN(META.SIGN, "SIGN"),
    SQRT(META.SQRT, "SQRT"),
    TRIM_SCALE(META.TRIM_SCALE, "TRIM_SCALE"),
    TRUNC(META.TRUNC, "TRUNC"),
    WIDTH_BUCKET (META.WIDTH_BUCKET , "width_bucket "),
    RANDOM(META.RANDOM, "RANDOM"),
    RANDOM_NORMAL(META.RANDOM_NORMAL, "RANDOM_NORMAL"),
    SET_SEED (META.SET_SEED , "SETSEED "),
    ACOS(META.ACOS, "ACOS"),
    ACOSD(META.ACOSD, "ACOSD"),
    ASIN(META.ASIN, "ASIN"),
    ASIND(META.ASIND, "ASIND"),
    ATAN(META.ATAN, "ATAN"),
    ATAND(META.ATAND, "ATAND"),
    ATAN2(META.ATAN2, "ATAN2"),
    ATAN2D(META.ATAN2D, "ATAN2D"),
    COS(META.COS, "COS"),
    COSD(META.COSD, "COSD"),
    COT(META.COT, "COT"),
    COTD(META.COTD, "COTD"),
    SIN(META.SIN, "SIN"),
    SIND(META.SIND, "SIND"),
    TAN(META.TAN, "TAN"),
    TAND(META.TAND, "TAND"),
    SINH(META.SINH, "SINH"),
    COSH(META.COSH, "COSH"),
    TANH(META.TANH, "TANH"),
    ASINH(META.ASINH, "ASINH"),
    ACOSH(META.ACOSH, "ACOSH"),
    ATANH(META.ATANH, "ATANH"),
    BTRIM(META.TRIM, "BTRIM"),
    BIT_LENGTH(META.BIT_LENGTH, "BIT_LENGTH"),
    CHAR_LENGTH(META.CHAR_LENGTH, "CHAR_LENGTH"),
    CHARACTER_LENGTH(META.CHARACTER_LENGTH, "CHARACTER_LENGTH"),
    LOWER(META.LOWER, "LOWER"),
    LPAD(META.LPAD, "LPAD"),
    LTRIM(META.LTRIM, "LTRIM"),
    NORMALIZE(META.NORMALIZE, "NORMALIZE"),
    OCTET_LENGTH(META.OCTET_LENGTH, "OCTET_LENGTH"),
    OVERLAY(META.OVERLAY, "OVERLAY"),
    POSITION(META.POSITION, "POSITION"),
    RPAD(META.RPAD, "RPAD"),
    RTRIM(META.RTRIM, "RTRIM"),
    SUBSTRING(META.SUBSTRING, "SUBSTRING"),
    TRIM(META.TRIM, "TRIM"),
    UNICODE_ASSIGNED(META.UNICODE_ASSIGNED, "UNICODE_ASSIGNED"),
    UPPER(META.UPPER, "UPPER"),
    ASCII(META.HEAD_ASCII, "ASCII"),
    CHR(META.CHR, "CHR"),
    CONCAT(META.CONCAT, "CONCAT"),
    CONCAT_WS(META.CONCAT_WS, "CONCAT_WS"),
    FORMAT(META.FORMAT, "FORMAT"),
    INITCAP(META.INITCAP, "INITCAP"),
    CASE_FOLD(META.CASE_FOLD, "CASEFOLD"),
    LEFT(META.LEFT, "LEFT"),
    LENGTH(META.LENGTH, "LENGTH"),
    MD5(META.MD5, "MD5"),
    PARSE_IDENT(META.PARSE_IDENT, "PARSE_IDENT"),
    CLIENT_ENCODING(META.CLIENT_ENCODING, "PG_CLIENT_ENCODING"),
    QUOTE_IDENT(META.QUOTE_IDENT, "QUOTE_IDENT"),
    QUOTE_LITERAL(META.QUOTE_LITERAL, "QUOTE_LITERAL"),
    QUOTE_NULLABLE(META.QUOTE_NULLABLE, "QUOTE_NULLABLE"),
    REGEXP_COUNT(META.REGEXP_COUNT, "REGEXP_COUNT"),
    OCCURRENCES_REGEX(META.REGEXP_COUNT, "OCCURRENCES_REGEX"),
    REGEXP_INSTR(META.REGEXP_POSITION, "REGEXP_INSTR"),
    POSITION_REGEX(META.REGEXP_POSITION, "POSITION_REGEX"),
    REGEXP_LIKE(META.REGEXP_LIKE, "REGEXP_LIKE"),
    REGEXP_MATCH(META.REGEXP_MATCH, "REGEXP_MATCH"),
    REGEXP_MATCHES(META.REGEXP_MATCHES, "REGEXP_MATCHES"),
    REGEXP_REPLACE(META.REGEXP_REPLACE, "REGEXP_REPLACE"),
    TRANSLATE_REGEX(META.REGEXP_REPLACE, "TRANSLATE_REGEX"),
    REGEXP_SPLIT_TO_ARRAY(META.REGEXP_SPLIT_TO_ARRAY, "REGEXP_SPLIT_TO_ARRAY"),
    REGEXP_SPLIT_TO_TABLE(META.REGEXP_SPLIT_TO_TABLE, "REGEXP_SPLIT_TO_TABLE"),
    REGEXP_SUBSTR(META.REGEXP_SUBSTR, "REGEXP_SUBSTR"),
    SUBSTRING_REGEX(META.REGEXP_SUBSTR, "SUBSTRING_REGEX"),
    REPEAT(META.REPEAT, "REPEAT"),
    REPLACE(META.REPLACE, "REPLACE"),
    REVERSE(META.REVERSE, "REVERSE"),
    RIGHT(META.RIGHT, "RIGHT"),
    SPLIT_PART(META.SPLIT_PART, "SPLIT_PART"),
    STARTS_WITH(META.STARTS_WITH, "STARTS_WITH"),
    STRING_TO_TABLE(META.STRING_TO_TABLE, "STRING_TO_TABLE"),
    STRING_POSITION(META.STRING_POSITION, "STRPOS"),
    SUBSTR(META.SUBSTR, "SUBSTR"),
    TO_ASCII(META.ASCII, "TO_ASCII"),
    TO_BIN(META.BINARY, "TO_BIN"),
    TO_HEX(META.HEX, "to_hex"),
    TO_OCT(META.OCT, "TO_OCT"),
    TRANSLATE(META.TRANSLATE, "TRANSLATE"),
    UNI_STR(META.UNICODE_STRING, "UNISTR"),
    STRING_FORMAT(META.STRING_FORMAT, "FORMAT"),
    BIT_COUNT(META.BIT_COUNT, "BIT_COUNT"),
    CRC32(META.CRC32, "CRC32"),
    CRC32C(META.CRC32C, "CRC32C"),
    GET_BIT(META.GET_BIT, "GET_BIT"),
    GET_BYTE(META.GET_BYTE, "GET_BYTE"),
    SET_BIT(META.SET_BIT, "SET_BIT"),
    SET_BYTE(META.SET_BYTE, "SET_BYTE"),
    SHA224(META.SHA224, "SHA224"),
    SHA256(META.SHA256, "SHA256"),
    SHA384(META.SHA384, "SHA384"),
    SHA512(META.SHA512, "SHA512"),
    CONVERT(META.CONVERT, "CONVERT"),
    CONVERT_FROM(META.CONVERT_FROM, "CONVERT_FROM"),
    CONVERT_TO(META.CONVERT_TO, "CONVERT_TO"),
    ENCODE(META.ENCODE, "ENCODE"),
    DECODE(META.DECODE, "DECODE"),
    TO_CHAR(META.TO_CHAR, "TO_CHAR"),
    TO_DATE(META.TO_DATE, "TO_DATE"),
    TO_NUMBER(META.TO_NUMBER, "TO_NUMBER"),
    TO_TIMESTAMP (META.TO_TIMESTAMP , "TO_TIMESTAMP "),
    CURRENT_DATE(META.CURRENT_DATE, "CURRENT_DATE"),
    CURRENT_TIME(META.CURRENT_TIME, "CURRENT_TIME"),
    CURRENT_TIMESTAMP(META.CURRENT_TIMESTAMP, "CURRENT_TIMESTAMP"),
    DATE_ADD(META.DATE_ADD, "DATE_ADD"),
    DATE_BIN(META.DATE_BIN, "DATE_BIN"),
    DATE_PART(META.DATE_PART, "DATE_PART"),
    DATE_SUBTRACT(META.DATE_SUB, "DATE_SUBTRACT"),
    DATE_TRUNCATE(META.DATE_TRUNCATE, "DATE_TRUNC"),
    EXTRACT(META.EXTRACT, "EXTRACT"),
    IS_FINITE(META.IS_FINITE, "ISFINITE"),
    JUSTIFY_DAYS(META.JUSTIFY_DAYS, "JUSTIFY_DAYS"),
    JUSTIFY_HOURS(META.JUSTIFY_HOURS, "JUSTIFY_HOURS"),
    JUSTIFY_INTERVAL(META.JUSTIFY_INTERVAL, "JUSTIFY_INTERVAL"),
    LOCALTIME(META.CURRENT_TIME, "LOCALTIME"),
    LOCALTIMESTAMP(META.CURRENT_TIMESTAMP, "LOCALTIMESTAMP"),
    MAKE_DATE(META.MAKE_DATE, "MAKE_DATE"),
    MAKE_INTERVAL(META.MAKE_INTERVAL, "MAKE_INTERVAL"),
    MAKE_TIME(META.MAKE_TIME, "MAKE_TIME"),
    MAKE_TIMESTAMP(META.MAKE_TIMESTAMP, "MAKE_TIMESTAMP"),
    NOW(META.NOW, "NOW"),
    STATEMENT_TIMESTAMP(META.CURRENT_TIMESTAMP, "STATEMENT_TIMESTAMP"),
    CURRENT_DATETIME(META.CURRENT_DATETIME, "TIMEOFDAY"),
    TRANSACTION_TIMESTAMP(META.TRANSACTION_TIMESTAMP, "TRANSACTION_TIMESTAMP"),
    DATE_TRUNC(META.DATE_TRUNCATE, "DATE_TRUNC"),
    SLEEP(META.SLEEP, "PG_SLEEP "),
    SLEEP_INTERVAL(META.SLEEP_INTERVAL, "PG_SLEEP_FOR"),
    SLEEP_UNTIL(META.SLEEP_UNTIL, "PG_SLEEP_UNTIL"),
    ENUM_FIRST(META.ENUM_FIRST, "ENUM_FIRST"),
    ENUM_LAST(META.ENUM_LAST, "ENUM_LAST"),
    ENUM_RANGE(META.ENUM_RANGE, "ENUM_RANGE"),
    GEOMETRIC_TYPE(META.ST_GEOMETRY_TYPE, "GEOMETRIC_TYPE"),
    AREA(META.AREA, "AREA"),
    CENTER(META.CENTER, "CENTER"),
    DIAGONAL(META.DIAGONAL, "DIAGONAL"),
    DIAMETER(META.DIAMETER, "DIAMETER"),
    BOX_HEIGHT(META.BOX_HEIGHT, "HEIGHT"),
    IS_CLOSED(META.ST_IS_CLOSED, "ISCLOSED"),
    IS_OPEN(META.ST_IS_OPEN, "ISOPEN"),
    POINT_COUNT(META.ST_POINT_COUNT, "NPOINTS"),
    PATH_CLOSE(META.ST_PATH_CLOSE, "PCLOSE"),
    PATH_OPEN(META.ST_PATH_CLOSE, "POPEN"),

    RADIUS(META.RADIUS, "RADIUS"),
    SLOPE(META.SLOPE, "SLOPE"),
    BOX_WIDTH(META.BOX_WIDTH, "WIDTH"),
    INNER_BOX(META.INNER_BOX, "BOX"),
    BOX_BOUND(META.BOX_BOUND, "BOUND_BOX"),
    BOX_CIRCLE(META.BOX_CIRCLE, "CIRCLE"),
    POINT_TO_LINE_STRING(META.POINT_TO_LINE_STRING, "LINE"),
    LSEG(META.LSEG, "LSEG"),
    TO_PATH(META.TO_PATH, "path"),
    TO_POINT(META.CENTER_POINT, "POINT"),
    TO_POLYGON(META.TO_POLYGON, "POLYGON"),
    IP_ABBREV(META.IP_ABBREV, "ABBREV"),
    IP_BROADCAST(META.IP_BROADCAST, "BROADCAST"),
    IP_VERSION(META.IP_VERSION, "family"),
    IP_HOST(META.IP_HOST, "HOST"),
    IP_HOST_MASK(META.IP_HOST_MASK, "HOSTMASK"),
    IP_INET_MERGE(META.IP_INET_MERGE, "INET_MERGE"),
    IP_INET_SAME_FAMILY(META.IP_INET_SAME_FAMILY, "INET_SAME_FAMILY"),
    IP_MASK_LENGTH(META.IP_MASK_LENGTH, "MASKLEN"),
    IP_NETMASK(META.IP_NETMASK, "NETMASK"),
    IP_NETWORK(META.IP_NETWORK, "NETWORK"),
    IP_SET_MASK_LENGTH(META.IP_SET_MASK_LENGTH, "SET_MASKLEN"),
    IP_TEXT(META.IP_TEXT, "TEXT"),
    ARRAY_TO_TSVECTOR (META.ARRAY_TO_TSVECTOR, "ARRAY_TO_TSVECTOR"),
    GET_CURRENT_TS_CONFIG(META.GET_CURRENT_TS_CONFIG, "GET_CURRENT_TS_CONFIG"),
    NUM_NODE (META.NUM_NODE, "NUMNODE"),
    PLAIN_TO_TSQUERY(META.PLAIN_TO_TSQUERY, "PLAINTO_TSQUERY"),
    PHRASE_TO_TSQUERY (META.PHRASE_TO_TSQUERY, "PHRASETO_TSQUERY"),
    WEBSEARCH_TO_TSQUERY(META.WEBSEARCH_TO_TSQUERY, "WEBSEARCH_TO_TSQUERY"),
    QUERY_TREE(META.QUERY_TREE, "QUERYTREE"),
    SET_WEIGHT(META.SET_WEIGHT, "SETWEIGHT"),
    TO_TS_QUERY(META.TO_TS_QUERY, "TO_TSQUERY"),
    TO_TS_VECTOR(META.TO_TS_VECTOR, "TO_TSVECTOR"),
    JSON_TO_TS_VECTOR(META.JSON_TO_TS_VECTOR, "JSON_TO_TSVECTOR"),
    TS_DELETE(META.TS_DELETE, "TS_DELETE"),
    TS_FILTER(META.TS_FILTER, "TS_FILTER"),
    TS_HEADLINE(META.TS_HEADLINE, "TS_HEADLINE"),
    TS_RANK(META.TS_RANK, "TS_RANK"),
    TS_RANK_CD(META.TS_RANK_CD, "TS_RANK_CD"),
    TS_REWRITE(META.TS_REWRITE, "TS_REWRITE"),
    TS_QUERY_PHRASE(META.TS_QUERY_PHRASE, "TSQUERY_PHRASE"),
    TS_VECTOR_TO_ARRAY(META.TS_VECTOR_TO_ARRAY, "TSVECTOR_TO_ARRAY"),
    UNNEST(META.UNNEST, "UNNEST"),
    TS_DEBUG(META.TS_DEBUG, "TS_DEBUG"),
    TS_LEXIZE(META.TS_LEXIZE, "TS_LEXIZE"),
    TS_PARSE(META.TS_PARSE, "TS_PARSE"),
    TS_TOKEN_TYPE(META.TS_TOKEN_TYPE, "TS_TOKEN_TYPE"),
    TS_STAT(META.TS_STAT, "TS_STAT"),
    UUIDV4(META.UUID, "UUIDV4"),
    UUIDV7(META.UUID , "UUIDV7"),
    UUID_EXTRACT_TIMESTAMP(META.UUID_EXTRACT_TIMESTAMP, "UUID_EXTRACT_TIMESTAMP"),
    UUID_EXTRACT_VERSION(META.UUID_EXTRACT_VERSION, "UUID_EXTRACT_VERSION"),
    XML_TO_TEXT(META.XML_TO_TEXT, "XMLTEXT"),
    XML_COMMENT(META.XML_COMMENT, "XMLCOMMENT"),
    XML_CONCAT(META.XML_CONCAT, "XMLCONCAT"),
    XML_ELEMENT(META.XML_ELEMENT, "XMLELEMENT"),
    XML_FOREST(META.XML_FOREST, "xmlforest"),
    XML_PI(META.XML_PI, "XMLPI"),
    XML_ROOT(META.XML_ROOT, "XMLROOT"),
    XML_AGG(META.XML_AGG, "XMLAGG"),
    XML_EXISTS(META.XML_EXISTS, "XMLEXISTS"),
    XML_IS_WELL_FORMED(META.XML_IS_WELL_FORMED, "XML_IS_WELL_FORMED"),
    XML_IS_WELL_FORMED_DOCUMENT(META.XML_IS_WELL_FORMED_DOCUMENT, "XML_IS_WELL_FORMED_DOCUMENT"),
    XML_IS_WELL_FORMED_CONTENT(META.XML_IS_WELL_FORMED_CONTENT, "XML_IS_WELL_FORMED_CONTENT"),
    XPATH(META.XPATH, "XPATH"),
    XPATH_EXISTS(META.XPATH_EXISTS, "XPATH_EXISTS"),
    XML_TABLE(META.XML_TABLE, "XMLTABLE"),
    TABLE_TO_XML(META.TABLE_TO_XML, "TABLE_TO_XML"),
    QUERY_TO_XML(META.QUERY_TO_XML, "QUERY_TO_XML"),
    CURSOR_TO_XML(META.CURSOR_TO_XML, "CURSOR_TO_XML"),
    TABLE_TO_XML_SCHEMA(META.TABLE_TO_XML_SCHEMA, "TABLE_TO_XMLSCHEMA"),
    QUERY_TO_XML_SCHEMA(META.QUERY_TO_XML_SCHEMA, "QUERY_TO_XMLSCHEMA"),
    CURSOR_TO_XM_SCHEMA(META.CURSOR_TO_XML_SCHEMA, "CURSOR_TO_XMLSCHEMA"),
    TABLE_TO_XML_AND_XML_SCHEMA(META.TABLE_TO_XML_AND_XML_SCHEMA, "TABLE_TO_XML_AND_XMLSCHEMA"),
    QUERY_TO_XML_AND_XML_SCHEMA(META.QUERY_TO_XML_AND_XML_SCHEMA, "QUERY_TO_XML_AND_XMLSCHEMA"),
    SCHEMA_TO_XML(META.SCHEMA_TO_XML, "SCHEMA_TO_XML"),
    SCHEMA_TO_XML_SCHEMA(META.SCHEMA_TO_XML_SCHEMA, "SCHEMA_TO_XMLSCHEMA"),
    SCHEMA_TO_XML_AND_XML_SCHEMA(META.SCHEMA_TO_XML_AND_XML_SCHEMA, "SCHEMA_TO_XML_AND_XMLSCHEMA"),
    DATABASE_TO_XML(META.DATABASE_TO_XML, "DATABASE_TO_XML"),
    DATABASE_TO_XML_SCHEMA(META.DATABASE_TO_XML_SCHEMA, "DATABASE_TO_XMLSCHEMA"),
    DATABASE_TO_XML_AND_XML_SCHEMA(META.DATABASE_TO_XML_AND_XML_SCHEMA, "DATABASE_TO_XML_AND_XMLSCHEMA"),
    TO_JSON(META.TO_JSON, "TO_JSON"),
    TO_JSONB(META.TO_JSONB, "TO_JSONB"),
    JSON_ARRAY(META.JSON_ARRAY, "JSON_ARRAY"),
    ARRAY_TO_JSON(META.ARRAY_TO_JSON, "ARRAY_TO_JSON"),
    ROW_TO_JSON(META.ROW_TO_JSON, "ROW_TO_JSON"),
    JSON_BUILD_ARRAY(META.JSON_BUILD_ARRAY, "JSON_BUILD_ARRAY"),
    JSON_BUILD_OBJECT(META.JSON_BUILD_OBJECT, "JSON_BUILD_OBJECT"),
    JSON_OBJECT(META.JSON_OBJECT, "JSON_OBJECT"),
    JSONB_OBJECT(META.JSONB_OBJECT, "JSONB_OBJECT"),
    JSON_SCALAR(META.JSON_SCALAR, "JSON_SCALAR"),
    JSON_SERIALIZE(META.JSON_SERIALIZE, "JSON_SERIALIZE"),
    JSON_ARRAY_ELEMENTS(META.JSON_ARRAY_ELEMENTS, "JSON_ARRAY_ELEMENTS"),
    JSONB_ARRAY_ELEMENTS(META.JSONB_ARRAY_ELEMENTS, "JSONB_ARRAY_ELEMENTS"),
    JSON_ARRAY_ELEMENTS_TEXT(META.JSON_ARRAY_ELEMENTS_TEXT, "JSON_ARRAY_ELEMENTS_TEXT"),
    JSONB_ARRAY_ELEMENTS_TEXT(META.JSONB_ARRAY_ELEMENTS_TEXT, "JSONB_ARRAY_ELEMENTS_TEXT"),
    JSON_ARRAY_LENGTH(META.JSON_ARRAY_LENGTH, "JSON_ARRAY_LENGTH"),
    JSONB_ARRAY_LENGTH(META.JSONB_ARRAY_LENGTH, "JSONB_ARRAY_LENGTH"),
    JSON_EACH(META.JSON_EACH, "JSON_EACH"),
    JSONB_EACH(META.JSONB_EACH, "JSONB_EACH"),
    JSON_EACH_TEXT(META.JSON_EACH_TEXT, "JSON_EACH_TEXT"),
    JSONB_EACH_TEXT(META.JSONB_EACH_TEXT, "JSONB_EACH_TEXT"),
    JSON_EXTRACT_PATH(META.JSON_EXTRACT_PATH, "JSON_EXTRACT_PATH"),
    JSONB_EXTRACT_PATH(META.JSONB_EXTRACT_PATH, "JSONB_EXTRACT_PATH"),
    JSON_EXTRACT_PATH_TEXT(META.JSON_EXTRACT_PATH_TEXT, "JSON_EXTRACT_PATH_TEXT"),
    JSONB_EXTRACT_PATH_TEXT(META.JSONB_EXTRACT_PATH_TEXT, "JSONB_EXTRACT_PATH_TEXT"),
    JSON_OBJECT_KEYS(META.JSON_OBJECT_KEYS, "JSON_OBJECT_KEYS"),
    JSONB_OBJECT_KEYS(META.JSONB_OBJECT_KEYS, "JSONB_OBJECT_KEYS"),
    JSON_POPULATE_RECORD(META.JSON_POPULATE_RECORD, "JSON_POPULATE_RECORD"),
    JSONB_POPULATE_RECORD(META.JSONB_POPULATE_RECORD, "JSONB_POPULATE_RECORD"),
    JSONB_POPULATE_RECORD_VALID (META.JSONB_POPULATE_RECORD_VALID , "JSONB_POPULATE_RECORD_VALID "),
    JSON_POPULATE_RECORDSET(META.JSON_POPULATE_RECORDSET, "JSON_POPULATE_RECORDSET"),
    JSONB_POPULATE_RECORDSET(META.JSONB_POPULATE_RECORDSET, "JSONB_POPULATE_RECORDSET"),
    JSON_TO_RECORD(META.JSON_TO_RECORD, "JSON_TO_RECORD"),
    JSONB_TO_RECORD(META.JSONB_TO_RECORD, "JSONB_TO_RECORD"),
    JSON_TO_RECORDSET(META.JSON_TO_RECORDSET, "JSON_TO_RECORDSET"),
    JSONB_TO_RECORDSET(META.JSONB_TO_RECORDSET, "JSONB_TO_RECORDSET"),
    JSONB_SET(META.JSONB_SET, "JSONB_SET"),
    JSONB_SET_LAX(META.JSONB_SET_LAX, "JSONB_SET_LAX"),
    JSONB_INSERT(META.JSONB_INSERT, "JSONB_INSERT"),
    JSON_STRIP_NULLS(META.JSON_STRIP_NULLS, "JSON_STRIP_NULLS"),
    JSONB_STRIP_NULLS(META.JSONB_STRIP_NULLS, "JSONB_STRIP_NULLS"),
    JSONB_PATH_EXISTS(META.JSONB_PATH_EXISTS, "JSONB_PATH_EXISTS"),
    JSONB_PATH_MATCH(META.JSONB_PATH_MATCH, "JSONB_PATH_MATCH"),
    JSONB_PATH_QUERY(META.JSONB_PATH_QUERY, "JSONB_PATH_QUERY"),
    JSONB_PATH_QUERY_ARRAY(META.JSONB_PATH_QUERY_ARRAY, "JSONB_PATH_QUERY_ARRAY"),
    JSONB_PATH_QUERY_FIRST(META.JSONB_PATH_QUERY_FIRST, "JSONB_PATH_QUERY_FIRST"),
    JSONB_PATH_EXISTS_TZ(META.JSONB_PATH_EXISTS_TZ, "JSONB_PATH_EXISTS_TZ"),
    JSONB_PATH_MATCH_TZ(META.JSONB_PATH_MATCH_TZ, "JSONB_PATH_MATCH_TZ"),
    JSONB_PATH_QUERY_TZ(META.JSONB_PATH_QUERY_TZ, "JSONB_PATH_QUERY_TZ"),
    JSONB_PATH_QUERY_ARRAY_TZ(META.JSONB_PATH_QUERY_ARRAY_TZ, "JSONB_PATH_QUERY_ARRAY_TZ"),
    JSONB_PATH_QUERY_FIRST_TZ(META.JSONB_PATH_QUERY_FIRST_TZ, "JSONB_PATH_QUERY_FIRST_TZ"),
    JSONB_PRETTY(META.JSONB_PRETTY, "JSONB_PRETTY"),
    JSON_TYPEOF(META.JSON_TYPEOF, "JSON_TYPEOF"),
    JSONB_TYPEOF(META.JSONB_TYPEOF, "JSONB_TYPEOF"),
    JSON_EXISTS(META.JSON_EXISTS, "JSON_EXISTS"),
    JSON_QUERY(META.JSON_QUERY, "JSON_QUERY"),
    JSON_VALUE(META.JSON_VALUE, "JSON_VALUE"),
    JSON_TABLE(META.JSON_TABLE, "JSON_TABLE"),
    NEXT_VAL(META.NEXT_VAL, "NEXTVAL"),
    SET_VAL(META.SET_VAL, "SETVAL"),
    CURR_VAL(META.CURR_VAL, "CURRVAL"),
    LAST_VAL(META.LAST_VAL, "LASTVAL"),
    COALESCE(META.COALESCE, "COALESCE"),
    IF_NULL(META.IF_NULL, "NULLIF"),
    GREATEST(META.GREATEST, "GREATEST"),
    LEAST(META.LEAST, "LEAST"),
    ARRAY_APPEND(META.ARRAY_APPEND, "ARRAY_APPEND"), //将元素附加到数组的末尾
    ARRAY_CAT(META.ARRAY_CAT, "ARRAY_CAT"), //连接两个数组
    ARRAY_DIMS(META.ARRAY_DIMS, "ARRAY_DIMS"), //数组维度的文本表示形式
    ARRAY_FILL(META.ARRAY_FILL, "ARRAY_FILL"), //一个数组，其中填充了给定值的副本
    ARRAY_LENGTH(META.ARRAY_LENGTH, "ARRAY_LENGTH"), //请求的数组维度的长度
    ARRAY_LOWER(META.ARRAY_LOWER, "ARRAY_LOWER"), //请求的数组维度的下限
    ARRAY_NDIMS(META.ARRAY_NDIMS, "ARRAY_NDIMS"), //数组的维度数
    ARRAY_POSITION(META.ARRAY_POSITION, "ARRAY_POSITION"), //数组中第二个参数首次出现的下标
    ARRAY_POSITIONS(META.ARRAY_POSITIONS, "ARRAY_POSITIONS"), //第一个参数中第二个参数所有出现的下标数组
    ARRAY_PREPEND(META.ARRAY_PREPEND, "ARRAY_PREPEND"), //在数组开头前加一个元素
    ARRAY_REMOVE(META.ARRAY_REMOVE, "ARRAY_REMOVE"), //从数组中移除所有等于给定值的元素
    ARRAY_REPLACE(META.ARRAY_REPLACE, "ARRAY_REPLACE"), //将等于第二个参数的每个数组元素替换为第三个参数
    ARRAY_REVERSE(META.ARRAY_REVERSE, "ARRAY_REVERSE"), //将数组的第一维度反转
    ARRAY_SAMPLE(META.ARRAY_SAMPLE, "ARRAY_SAMPLE"), //一个由 N 个元素组成的数组，从数组中随机选择
    ARRAY_SHUFFLE(META.ARRAY_SHUFFLE, "ARRAY_SHUFFLE"), //随机打乱数组的第一个维度
    ARRAY_SORT(META.ARRAY_SORT, "ARRAY_SORT"), //对数组的第一个维度进行排序
    ARRAY_TO_STRING(META.ARRAY_TO_STRING, "ARRAY_TO_STRING"), //将每个数组元素转换为其文本表示形式
    ARRAY_UPPER(META.ARRAY_UPPER, "ARRAY_UPPER"), //请求的数组维度的上限
    CARDINALITY(META.CARDINALITY, "CARDINALITY"), //数组中的元素总数
    TRIM_ARRAY(META.TRIM_ARRAY, "TRIM_ARRAY"), //通过删除最后 N 个元素来修剪数组
    IS_EMPTY(META.IS_EMPTY, "ISEMPTY"), //范围是否空
    LOWER_INC(META.LOWER_INC, "LOWER_INC"), //范围的下限是否包含
    UPPER_INC(META.UPPER_INC, "UPPER_INC"), //该范围的上限是否包含
    LOWER_INF(META.LOWER_INF, "LOWER_INF"), //范围是否有下限
    UPPER_INF(META.UPPER_INF, "UPPER_INF"), //范围是否有上限
    RANGE_MERGE(META.RANGE_MERGE, "RANGE_MERGE"), //计算包括整个多范围的最小范围
    MULTI_RANGE(META.MULTI_RANGE, "MULTIRANGE"), //仅包含给定范围的多范围
    ANY_VALUE(META.ANY_VALUE, "ANY_VALUE"), //
    ARRAY_AGG(META.ARRAY_AGG, "ARRAY_AGG"), //从非空输入值返回任意值
    AVG(META.AVG, "AVG"), //
    BIT_AND(META.BIT_AND, "BIT_AND"), //计算所有非空输入值的按位 AND
    BIT_OR(META.BIT_OR, "BIT_OR"), //
    BIT_XOR(META.BIT_XOR, "BIT_XOR"), //
    BOOL_AND(META.BOOL_AND, "BOOL_AND"), //
    BOOL_OR(META.BOOL_OR, "BOOL_OR"), //
    COUNT(META.COUNT, "COUNT"), //
    EVERY(META.EVERY, "EVERY"), //
    JSON_AGG(META.JSON_AGG, "JSON_AGG"), //将所有输入值（包括空值）收集到 JSON 数组中
    JSONB_AGG(META.JSONB_AGG, "JSONB_AGG"), //将所有输入值（包括空值）收集到 JSON 数组中
    JSON_AGG_STRICT(META.JSON_AGG_STRICT, "JSON_AGG_STRICT"), //将所有输入值（跳过 null）收集到 JSON 数组中
    JSONB_AGG_STRICT(META.JSONB_AGG_STRICT, "JSONB_AGG_STRICT"), //将所有输入值（跳过 null）收集到 JSON 数组中
    JSON_ARRAY_AGG(META.JSON_ARRAY_AGG, "JSON_ARRAYAGG"), //
    JSON_OBJECT_AGG(META.JSON_OBJECT_AGG, "JSON_OBJECTAGG"), //将所有键/值对收集到 JSON 对象中
    JSONB_OBJECT_AGG(META.JSONB_OBJECT_AGG, "JSONB_OBJECTAGG"), //将所有键/值对收集到 JSON 对象中
    JSON_OBJECT_AGG_STRICT(META.JSON_OBJECT_AGG_STRICT, "JSON_OBJECT_AGG_STRICT"), //将所有键/值对收集到 JSON 对象中
    JSONB_OBJECT_AGG_STRICT(META.JSONB_OBJECT_AGG_STRICT, "JSONB_OBJECT_AGG_STRICT"), //将所有键/值对收集到 JSON 对象中
    JSON_OBJECT_AGG_UNIQUE(META.JSON_OBJECT_AGG_UNIQUE, "JSON_OBJECT_AGG_UNIQUE"), //将所有键/值对收集到 JSON 对象中
    JSONB_OBJECT_AGG_UNIQUE(META.JSONB_OBJECT_AGG_UNIQUE, "JSONB_OBJECT_AGG_UNIQUE"), //将所有键/值对收集到 JSON 对象中
    JSON_OBJECT_AGG_UNIQUE_STRICT(META.JSON_OBJECT_AGG_UNIQUE_STRICT, "JSON_OBJECT_AGG_UNIQUE_STRICT"), //将所有键/值对收集到 JSON 对象中
    JSONB_OBJECT_AGG_UNIQUE_STRICT(META.JSONB_OBJECT_AGG_UNIQUE_STRICT, "JSONB_OBJECT_AGG_UNIQUE_STRICT"), //将所有键/值对收集到 JSON 对象中
    MAX(META.MAX, "MAX"), //
    MIN(META.MIN, "MIN"), //
    RANGE_AGG(META.RANGE_AGG, "RANGE_AGG"), //计算非空输入值的并集
    RANGE_INTERSECT_AGG(META.RANGE_INTERSECT_AGG, "RANGE_INTERSECT_AGG"), //计算非空输入值的交集
    STRING_AGG(META.STRING_AGG, "STRING_AGG"), //将非空输入值连接成字符串
    SUM(META.SUM, "SUM"), //
    CORR(META.CORR, "CORR"), //计算相关系数
    COVAR_POP(META.COVAR_POP, "COVAR_POP"), //计算总体协方差
    COVAR_SAMP (META.COVAR_SAMP , "COVAR_SAMP "), //计算样本协方差
    REGR_AVGX(META.REGR_AVGX, "REGR_AVGX"), //计算自变量 的平均值
    REGR_AVGY(META.REGR_AVGY, "REGR_AVGY"), //计算因变量 的平均值
    REGR_COUNT(META.REGR_COUNT, "REGR_COUNT"), //计算两个输入都为非空的行数
    REGR_INTERCEPT(META.REGR_INTERCEPT, "REGR_INTERCEPT"), //计算由 （X， Y） 对确定的最小二乘拟合线性方程的 y 截距
    REGR_R2(META.REGR_R2, "REGR_R2"), //计算相关系数的平方
    REGR_SLOPE(META.REGR_SLOPE, "REGR_SLOPE"), //计算由 （X， Y） 对确定的最小二乘拟合线性方程的斜率
    REGR_SXX(META.REGR_SXX, "REGR_SXX"), //计算自变量的“平方和”
    REGR_SXY(META.REGR_SXY, "REGR_SXY"), //计算独立乘时因变量的“乘积之和”
    REGR_SYY(META.REGR_SYY, "REGR_SYY"), //计算因变量的“平方和”
    STDDEV(META.STDDEV, "STDDEV"), //
    STDDEV_POP(META.STDDEV_POP, "STDDEV_POP"), //计算输入值的总体标准差
    STDDEV_SAMP(META.STDDEV_SAMP, "STDDEV_SAMP"), //计算输入值的样本标准差
    VARIANCE(META.VARIANCE, "VARIANCE"), //
    VAR_POP(META.VAR_POP, "VAR_POP"), //计算输入值的总体方差
    VAR_SAMP(META.VAR_SAMP, "VAR_SAMP"), //计算输入值的样本方差
    MODE(META.MODE, "MODE"), //
    PERCENTILE_CONT(META.PERCENTILE_CONT, "PERCENTILE_CONT"), //计算连续百分位数
    PERCENTILE_DISC(META.PERCENTILE_DISC, "PERCENTILE_DISC"), //计算离散百分位数
    RANK(META.RANK, "RANK"), //
    PERCENT_RANK (META.PERCENT_RANK , "PERCENT_RANK "), //
    DENSE_RANK(META.DENSE_RANK, "DENSE_RANK"), //
    CUME_DIST(META.CUME_DIST, "CUME_DIST"), //
    ROW_NUMBER(META.ROW_NUMBER, "ROW_NUMBER"), //
    NTILE(META.NTILE, "NTILE"), //
    LAG(META.LAG, "LAG"), //
    LEAD(META.LEAD, "LEAD"), //
    FIRST_VALUE(META.FIRST_VALUE, "FIRST_VALUE"), //
    LAST_VALUE(META.LAST_VALUE, "LAST_VALUE"), //
    NTH_VALUE(META.NTH_VALUE, "NTH_VALUE"), //
    MERGE_ACTION(META.MERGE_ACTION, "MERGE_ACTION"), //为当前行执行的合并作命令
    EXISTS(META.EXISTS, "EXISTS"), //
    GENERATE_SERIES (META.GENERATE_SERIES, "GENERATE_SERIES"), //生成从开始到停止的一系列值
    GENERATE_SUBSCRIPTS(META.GENERATE_SUBSCRIPTS, "GENERATE_SUBSCRIPTS"), //生成一个序列，其中包含给定数组的暗维的有效下标
    CURRENT_DATABASE(META.CURRENT_DATABASE, "CURRENT_DATABASE"), //
    CURRENT_CATALOG(META.CURRENT_CATALOG, "CURRENT_CATALOG"), //
    CURRENT_QUERY(META.CURRENT_QUERY, "CURRENT_QUERY"), //客户端提交的当前正在执行的查询的文本
    CURRENT_ROLE(META.CURRENT_ROLE, "CURRENT_ROLE"), //
    CURRENT_SCHEMAS(META.CURRENT_SCHEMAS, "CURRENT_SCHEMAS"), //
    CURRENT_SCHEMA(META.CURRENT_SCHEMA, "CURRENT_SCHEMA"), //
    CURRENT_USER(META.CURRENT_USER, "CURRENT_USER"), //
    INET_CLIENT_ADDR(META.INET_CLIENT_ADDR, "INET_CLIENT_ADDR"), //当前客户端的 IP 地址
    INET_CLIENT_PORT(META.INET_CLIENT_PORT, "INET_CLIENT_PORT"), //当前客户端的 IP 端口号
    INET_SERVER_ADDR(META.INET_SERVER_ADDR, "INET_SERVER_ADDR"), //服务器接受当前连接的 IP 地址
    INET_SERVER_PORT(META.INET_SERVER_PORT, "INET_SERVER_PORT"), //服务器接受当前连接的 IP 端口号
    BACKEND_PID(META.BACKEND_PID, "PG_BACKEND_PID"), //附加到当前会话的服务器进程的进程 ID
    BLOCKING_PIDS(META.BLOCKING_PIDS, "PG_BLOCKING_PIDS"), //阻止具有指定进程 ID 的服务器进程获取锁的会话的进程 ID 数组
    CONF_LOAD_TIME(META.CONF_LOAD_TIME, "PG_CONF_LOAD_TIME"), //服务器配置文件最后一次加载的时间
    CURRENT_LOGFILE(META.CURRENT_LOGFILE, "PG_CURRENT_LOGFILE"), //日志记录收集器当前使用的日志文件的路径名
    GET_LOADED_MODULES(META.GET_LOADED_MODULES, "PG_GET_LOADED_MODULES"), //加载到当前服务器会话中的可加载模块的列表
    MY_TEMP_SCHEMA(META.MY_TEMP_SCHEMA, "PG_MY_TEMP_SCHEMA"), //当前会话的临时模式的 OID
    IS_OTHER_TEMP_SCHEMA(META.IS_OTHER_TEMP_SCHEMA, "PG_IS_OTHER_TEMP_SCHEMA"), //如果给定的 OID 是另一个会话的临时架构的 OID
    JIT_AVAILABLE(META.JIT_AVAILABLE, "PG_JIT_AVAILABLE"), //JIT 编译器扩展可用
    NUMA_AVAILABLE(META.NUMA_AVAILABLE, "PG_NUMA_AVAILABLE"), //服务器已使用 NUMA 支持进行编译
    LISTENING_CHANNELS(META.LISTENING_CHANNELS, "PG_LISTENING_CHANNELS"), //当前会话正在侦听的异步通知通道的一组名
    NOTIFICATION_QUEUE_USAGE(META.NOTIFICATION_QUEUE_USAGE, "PG_NOTIFICATION_QUEUE_USAGE"), //异步通知队列的最大大小的分数
    POSTMASTER_START_TIME(META.POSTMASTER_START_TIME, "PG_POSTMASTER_START_TIME"), //服务器启动的时间
    SAFE_SNAPSHOT_BLOCKING_PIDS (META.SAFE_SNAPSHOT_BLOCKING_PIDS , "PG_SAFE_SNAPSHOT_BLOCKING_PIDS "), //阻止具有指定进程 ID 的服务器进程获取安全快照的会话的进程 ID 数组
    TRIGGER_DEPTH(META.TRIGGER_DEPTH, "PG_TRIGGER_DEPTH"), //触发器的当前嵌套级别
    SESSION_USER(META.SESSION_USER, "SESSION_USER"), //
    SYSTEM_USER(META.SYSTEM_USER, "SYSTEM_USER"), //
    HAS_ANY_COLUMN_PRIVILEGE(META.HAS_ANY_COLUMN_PRIVILEGE, "HAS_ANY_COLUMN_PRIVILEGE"), //用户是否对表的任何列具有权限
    HAS_COLUMN_PRIVILEGE(META.HAS_COLUMN_PRIVILEGE, "HAS_COLUMN_PRIVILEGE"), //用户是否对指定的表列具有权限
    HAS_DATABASE_PRIVILEGE(META.HAS_DATABASE_PRIVILEGE, "HAS_DATABASE_PRIVILEGE"), //用户是否具有数据库权限
    HAS_FOREIGN_DATA_WRAPPER_PRIVILEGE(META.HAS_FOREIGN_DATA_WRAPPER_PRIVILEGE, "HAS_FOREIGN_DATA_WRAPPER_PRIVILEGE"), //用户是否具有外部数据包装器的权限
    HAS_FUNCTION_PRIVILEGE(META.HAS_FUNCTION_PRIVILEGE, "HAS_FUNCTION_PRIVILEGE"), //用户是否具有功能权限
    HAS_LANGUAGE_PRIVILEGE(META.HAS_LANGUAGE_PRIVILEGE, "HAS_LANGUAGE_PRIVILEGE"), //用户是否具有语言权限
    HAS_LARGE_OBJECT_PRIVILEGE(META.HAS_LARGE_OBJECT_PRIVILEGE, "HAS_LARGEOBJECT_PRIVILEGE"), //用户是否具有大型对象的权限
    HAS_PARAMETER_PRIVILEGE(META.HAS_PARAMETER_PRIVILEGE, "HAS_PARAMETER_PRIVILEGE"), //用户是否具有配置参数的权限
    HAS_SCHEMA_PRIVILEGE(META.HAS_SCHEMA_PRIVILEGE, "HAS_SCHEMA_PRIVILEGE"), //用户是否具有架构权限
    HAS_SEQUENCE_PRIVILEGE(META.HAS_SEQUENCE_PRIVILEGE, "HAS_SEQUENCE_PRIVILEGE"), //用户是否具有序列权限
    HAS_SERVER_PRIVILEGE(META.HAS_SERVER_PRIVILEGE, "HAS_SERVER_PRIVILEGE"), //用户是否具有外部服务器的权限
    HAS_TABLE_PRIVILEGE (META.HAS_TABLE_PRIVILEGE , "HAS_TABLE_PRIVILEGE "), //用户是否具有表的权限
    AS_TABLESPACE_PRIVILEGE(META.AS_TABLESPACE_PRIVILEGE, "AS_TABLESPACE_PRIVILEGE"), //用户是否具有表空间权限
    HAS_TYPE_PRIVILEGE (META.HAS_TYPE_PRIVILEGE , "HAS_TYPE_PRIVILEGE "), //用户是否具有数据类型的权限
    HAS_ROLE(META.HAS_ROLE, "PG_HAS_ROLE"), //用户是否具有角色权限
    ROW_SECURITY_ACTIVE(META.ROW_SECURITY_ACTIVE, "ROW_SECURITY_ACTIVE"), //在当前用户和当前环境的上下文中，指定表的行级安全性是否处于活动状态
    ACL_DEFAULT(META.ACL_DEFAULT, "ACLDEFAULT"), //构造一个数组，该数组为属于具有 OID ownerId 的角色的类型类型对象的默认访问权限
    ACL_EXPLODE(META.ACL_EXPLODE, "ACLEXPLODE"), //将数组作为一组行返回。如果被授权者是伪角色 PUBLIC，则在被授权者列中用零表示
    MAKE_ACL_ITEM(META.MAKE_ACL_ITEM, "MAKEACLITEM"), //构造具有给定属性的属性
    COLLATION_IS_VISIBLE(META.COLLATION_IS_VISIBLE, "PG_COLLATION_IS_VISIBLE"), //排序规则在搜索路径中是否可见
    CONVERSION_IS_VISIBLE(META.CONVERSION_IS_VISIBLE, "PG_CONVERSION_IS_VISIBLE"), //转化在搜索路径中可见
    FUNCTION_IS_VISIBLE(META.FUNCTION_IS_VISIBLE, "PG_FUNCTION_IS_VISIBLE"),//搜索路径中功能可见
    OPERATOR_CLASS_IS_VISIBLE (META.OPERATOR_CLASS_IS_VISIBLE , "PG_OPCLASS_IS_VISIBLE "),//运算符类在搜索路径中可见
    OPERATOR_IS_VISIBLE(META.OPERATOR_IS_VISIBLE, "PG_OPERATOR_IS_VISIBLE"),//运算符在搜索路径中可见
    OPERATOR_FAMILY_IS_VISIBLE(META.OPERATOR_FAMILY_IS_VISIBLE, "PG_OPFAMILY_IS_VISIBLE"),//运算符族在搜索路径中是否可见
    STATISTICS_OBJ_IS_VISIBLE(META.STATISTICS_OBJ_IS_VISIBLE, "PG_STATISTICS_OBJ_IS_VISIBLE"),//统计对象在搜索路径中是否可见
    TABLE_IS_VISIBLE(META.TABLE_IS_VISIBLE, "PG_TABLE_IS_VISIBLE"),//表格在搜索路径中可见
    TS_CONFIG_IS_VISIBLE(META.TS_CONFIG_IS_VISIBLE, "PG_TS_CONFIG_IS_VISIBLE"),//文本搜索配置在搜索路径中是否可见
    TS_DICT_IS_VISIBLE(META.TS_DICT_IS_VISIBLE, "PG_TS_DICT_IS_VISIBLE"),//文本搜索词典在搜索路径中是否可见
    TS_PARSER_IS_VISIBLE(META.TS_PARSER_IS_VISIBLE, "PG_TS_PARSER_IS_VISIBLE"),//文本搜索解析器在搜索路径中是否可见
    TS_TEMPLATE_IS_VISIBLE(META.TS_TEMPLATE_IS_VISIBLE, "PG_TS_TEMPLATE_IS_VISIBLE"),//文本搜索模板在搜索路径中是否可见
    TYPE_IS_VISIBLE(META.TYPE_IS_VISIBLE, "PG_TYPE_IS_VISIBLE"),//类型（或域）在搜索路径中是否可见
    FORMAT_TYPE(META.FORMAT_TYPE, "FORMAT_TYPE"), //类型 OID 和可能的类型修饰符标识的数据类型的 SQL 名称
    BASE_TYPE(META.BASE_TYPE, "PG_BASETYPE"),
    CHAR_TO_ENCODING(META.CHAR_TO_ENCODING, "PG_CHAR_TO_ENCODING"), //将提供的编码名称转换为表示某些系统目录表中使用的内部标识符的整数
    ENCODING_TO_CHAR(META.ENCODING_TO_CHAR, "PG_ENCODING_TO_CHAR"), //将某些系统目录表中用作编码内部标识符的整数转换为可读的字符串
    GET_CATALOG_FOREIGN_KEYS(META.GET_CATALOG_FOREIGN_KEYS, "PG_GET_CATALOG_FOREIGN_KEYS"), //描述POSTGRESQL系统目录中存在的外键关系
    GET_CONSTRAINT_DEF(META.GET_CONSTRAINT_DEF, "PG_GET_CONSTRAINTDEF"), //重建约束的创建命令
    GET_EXPR(META.GET_EXPR, "PG_GET_EXPR"), //反编译存储在系统目录中的表达式的内部形式
    GET_FUNCTION_DEF(META.GET_FUNCTION_DEF, "PG_GET_FUNCTIONDEF"),//重建函数或过程的创建命令
    GET_FUNCTION_ARGUMENTS(META.GET_FUNCTION_ARGUMENTS, "PG_GET_FUNCTION_ARGUMENTS"),//以函数或过程需要出现的形式（包括默认值）重建函数或过程的参数列表
    GET_FUNCTION_IDENTITY_ARGUMENTS (META.GET_FUNCTION_IDENTITY_ARGUMENTS , "PG_GET_FUNCTION_IDENTITY_ARGUMENTS "), //重建识别函数或过程所需的参数列表
    GET_FUNCTION_RESULT (META.GET_FUNCTION_RESULT , "PG_GET_FUNCTION_RESULT "),//重建函数的子句
    GET_INDEX_DEF (META.GET_INDEX_DEF , "PG_GET_INDEXDEF "), //重建索引的创建命令
    GET_KEYWORDS(META.GET_KEYWORDS, "PG_GET_KEYWORDS"), //描述服务器识别的关键字
    GET_PART_KEY_DEF(META.GET_PART_KEY_DEF, "PG_GET_PARTKEYDEF"),//重建分区表的分区键的定义
    GET_RULE_DEF (META.GET_RULE_DEF , "PG_GET_RULEDEF "),//重建规则的创建命令
    GET_SERIAL_SEQUENCE (META.GET_SERIAL_SEQUENCE , "PG_GET_SERIAL_SEQUENCE "), //与列关联的序列的名称
    GET_STATISTICS_OBJECT_DEF(META.GET_STATISTICS_OBJECT_DEF, "PG_GET_STATISTICSOBJDEF"), //重建扩展统计信息对象的创建命令
    GET_TRIGGER_DEF(META.GET_TRIGGER_DEF, "PG_GET_TRIGGERDEF"), //重建触发器的创建命令
    GET_USER_BY_ID(META.GET_USER_BY_ID, "PG_GET_USERBYID"), //OID 的角色名称
    GET_VIEW_DEF(META.GET_VIEW_DEF, "PG_GET_VIEWDEF"), //重建视图或具体化视图的基础命令
    INDEX_COLUMN_HAS_PROPERTY(META.INDEX_COLUMN_HAS_PROPERTY, "PG_INDEX_COLUMN_HAS_PROPERTY"), //测试索引列是否具有命名属性
    INDEX_HAS_PROPERTY(META.INDEX_HAS_PROPERTY, "PG_INDEX_HAS_PROPERTY"),//测试索引是否具有命名属性
    INDEX_AM_HAS_PROPERTY(META.INDEX_AM_HAS_PROPERTY, "PG_INDEXAM_HAS_PROPERTY"), //测试索引访问方法是否具有命名属性
    OPTIONS_TO_TABLE(META.OPTIONS_TO_TABLE, "PG_OPTIONS_TO_TABLE"), //存储选项集
    SETTINGS_GET_FLAGS(META.SETTINGS_GET_FLAGS, "PG_SETTINGS_GET_FLAGS"), //给定 GUC 关联的标志数组
    TABLESPACE_DATABASES(META.TABLESPACE_DATABASES, "PG_TABLESPACE_DATABASES"), //在指定表空间中存储对象的数据库的 OID 集
    TABLESPACE_LOCATION(META.TABLESPACE_LOCATION, "PG_TABLESPACE_LOCATION"), //此表空间所在的文件系统路径
    TYPEOF(META.TYPEOF, "PG_TYPEOF"), //传递给它的值的数据类型的 OID
    TO_REG_CLASS(META.TO_REG_CLASS, "TO_REGCLASS"), //将文本关系名称转换为其 OID
    TO_REG_COLLATION(META.TO_REG_COLLATION, "TO_REGCOLLATION"), //将文本排序规则名称转换为其 OID
    TO_REG_NAMESPACE(META.TO_REG_NAMESPACE, "TO_REGNAMESPACE"), //将文本架构名称转换为其 OID
    TO_REG_OPER(META.TO_REG_OPERATOR, "TO_REGOPER"), //将文本运算符名称转换为其 OID
    TO_REG_OPERATOR(META.TO_REG_OPERATOR, "TO_REGOPERATOR"), //将文本运算符名称（带有参数类型）转换为其 OID
    TO_REG_PROC(META.TO_REG_PROC, "TO_REGPROC"), //将文本函数或过程名称转换为其 OID
    TO_REG_PROCEDURE(META.TO_REG_PROCEDURE, "TO_REGPROCEDURE"), //将文本函数或过程名称（带有参数类型）转换为其 OID
    TO_REG_ROLE(META.TO_REG_ROLE, "TO_REGROLE"), //将文本角色名称转换为其 OID
    TO_REG_TYPE(META.TO_REG_TYPE, "TO_REGTYPE"), //解析文本字符串，从中提取潜在的类型名称，并将该名称转换为类型 OID
    TO_REG_TYPE_MOD(META.TO_REG_TYPE_MOD, "TO_REGTYPEMOD"), //解析文本字符串，从中提取潜在的类型名称，并翻译其类型修饰符
    GET_ACL(META.GET_ACL, "PG_GET_ACL"), //数据库对象的 ACL
    DESCRIBE_OBJECT(META.DESCRIBE_OBJECT, "PG_DESCRIBE_OBJECT"), //由目录 OID、对象 OID 和子对象 ID 标识的数据库对象的文本描述
    IDENTIFY_OBJECT(META.IDENTIFY_OBJECT, "PG_IDENTIFY_OBJECT"), //包含足够信息的行，以唯一标识由目录 OID、对象 OID 和子对象 ID 指定的数据库对象
    IDENTIFY_OBJECT_AS_ADDRESS(META.IDENTIFY_OBJECT_AS_ADDRESS, "PG_IDENTIFY_OBJECT_AS_ADDRESS"), //以唯一标识由目录 OID、对象 OID 和子对象 ID 指定的数据库对象
    GET_OBJECT_ADDRESS(META.GET_OBJECT_ADDRESS, "PG_GET_OBJECT_ADDRESS"), //以唯一标识类型代码以及对象名称和参数数组指定的数据库对象
    COL_DESCRIPTION(META.COL_DESCRIPTION, "COL_DESCRIPTION"), //列注释
    OBJ_DESCRIPTION(META.OBJ_DESCRIPTION, "OBJ_DESCRIPTION"), //OID 指定的数据库对象的注释和包含的系统目录的名称
    SH_OBJECT_DESCRIPTION(META.SH_OBJECT_DESCRIPTION, "SHOBJ_DESCRIPTION"), //OID 指定的共享数据库对象的注释和包含系统目录的名称
    INPUT_IS_VALID(META.INPUT_IS_VALID, "PG_INPUT_IS_VALID"), //测试给定字符串是否是指定数据类型的有效输入
    INPUT_ERROR_INFO(META.INPUT_ERROR_INFO, "PG_INPUT_ERROR_INFO"), //测试给定字符串是否是指定数据类型的有效输入
    MXID_AGE(META.MXID_AGE, "MXID_AGE"), //当前 MULTIXACTS 计数器之间的 MULTIXACTS ID 数
    CURRENT_XACT_ID(META.CURRENT_XACT_ID, "PG_CURRENT_XACT_ID"), //当前事务的 ID
    CURRENT_XACT_ID_IF_ASSIGNED(META.CURRENT_XACT_ID_IF_ASSIGNED, "PG_CURRENT_XACT_ID_IF_ASSIGNED"), //当前事务的 ID
    XACT_STATUS(META.XACT_STATUS, "PG_XACT_STATUS"), //最近事务的提交状态
    CURRENT_SNAPSHOT(META.CURRENT_SNAPSHOT, "PG_CURRENT_SNAPSHOT"), //当前快照
    SNAPSHOT_XIP(META.SNAPSHOT_XIP, "PG_SNAPSHOT_XIP"), //快照中包含的一组正在进行的事务 ID
    SNAPSHOT_XMAX(META.SNAPSHOT_XMAX, "PG_SNAPSHOT_XMAX"), //快照的XMAX
    SNAPSHOT_XMIN(META.SNAPSHOT_XMIN, "PG_SNAPSHOT_XMIN"), //快照的XMIN
    VISIBLE_IN_SNAPSHOT(META.VISIBLE_IN_SNAPSHOT, "PG_VISIBLE_IN_SNAPSHOT"), //事务 ID 是否可见
    XACT_COMMIT_TIMESTAMP(META.XACT_COMMIT_TIMESTAMP, "PG_XACT_COMMIT_TIMESTAMP"), //事务的提交时间戳
    XACT_COMMIT_TIMESTAMP_ORIGIN (META.XACT_COMMIT_TIMESTAMP_ORIGIN , "PG_XACT_COMMIT_TIMESTAMP_ORIGIN "), //事务的提交时间戳和复制源
    LAST_COMMITTED_XACT(META.LAST_COMMITTED_XACT, "PG_LAST_COMMITTED_XACT"), //最新提交事务的事务 ID、提交时间戳和复制源
    CONTROL_CHECKPOINT(META.CONTROL_CHECKPOINT, "PG_CONTROL_CHECKPOINT"), //有关当前检查点状态的信息
    CONTROL_SYSTEM(META.CONTROL_SYSTEM, "PG_CONTROL_SYSTEM"), //有关当前控制文件状态的信息
    CONTROL_INIT(META.CONTROL_INIT, "PG_CONTROL_INIT"), //关于簇初始化状态的信息
    CONTROL_RECOVERY(META.CONTROL_RECOVERY, "PG_CONTROL_RECOVERY"), //恢复状态的信息
    VERSION(META.VERSION, "VERSION"), //服务器版本的字符串
    UNICODE_VERSION(META.UNICODE_VERSION, "UNICODE_VERSION"), //使用的 UNICODE 版本
    ICU_UNICODE_VERSION(META.ICU_UNICODE_VERSION, "ICU_UNICODE_VERSION"), //ICU 使用的 UNICODE 版本
    AVAILABLE_WAL_SUMMARIES(META.AVAILABLE_WAL_SUMMARIES, "PG_AVAILABLE_WAL_SUMMARIES"), //关数据目录中存在的 WAL 摘要文件的信息
    WAL_SUMMARY_CONTENTS(META.WAL_SUMMARY_CONTENTS, "PG_WAL_SUMMARY_CONTENTS"), //有关由TLI标识的单个WAL摘要文件的内容以及开始和结束LSN的信息
    GET_WAL_SUMMARIZER_STATE(META.GET_WAL_SUMMARIZER_STATE, "PG_GET_WAL_SUMMARIZER_STATE"), //有关WAL摘要器进度的信息
    CURRENT_SETTING(META.CURRENT_SETTING, "CURRENT_SETTING"), //当前设置的值SETTING_NAME
    SET_CONFIG(META.SET_CONFIG, "SET_CONFIG"), //将参数 SETTING_NAME 设为 NEW_VALUE，并返回该值
    CANCEL_BACKEND(META.CANCEL_BACKEND, "PG_CANCEL_BACKEND"), //取消其后端进程具有指定进程 ID 的会话的当前查询
    LOG_BACKEND_MEMORY_CONTEXTS(META.LOG_BACKEND_MEMORY_CONTEXTS, "PG_LOG_BACKEND_MEMORY_CONTEXTS"), //请求日志后端的内存上下文，并指定进程ID
    RELOAD_CONF(META.RELOAD_CONF, "PG_RELOAD_CONF"), //使 POSTGRESQL 服务器的所有进程重新加载其配置文件
    ROTATE_LOGFILE(META.ROTATE_LOGFILE, "PG_ROTATE_LOGFILE"), //提示日志文件管理器立即切换到新的输出文件
    TERMINATE_BACKEND(META.TERMINATE_BACKEND, "PG_TERMINATE_BACKEND"), //终止其后端进程具有指定进程 ID 的会话
    CREATE_RESTORE_POINT(META.CREATE_RESTORE_POINT, "PG_CREATE_RESTORE_POINT"), //在预写日志中创建一个命名标记记录
    CURRENT_WAL_FLUSH_LSN(META.CURRENT_WAL_FLUSH_LSN, "PG_CURRENT_WAL_FLUSH_LSN"), //当前预写日志刷新位置
    CURRENT_WAL_INSERT_LSN(META.CURRENT_WAL_INSERT_LSN, "PG_CURRENT_WAL_INSERT_LSN"), //当前预写日志插入位置
    CURRENT_WAL_LSN(META.CURRENT_WAL_LSN, "PG_CURRENT_WAL_LSN"), //当前预写日志写入位置
    BACKUP_START(META.BACKUP_START, "PG_BACKUP_START"), //准备服务器开始联机备份
    BACKUP_STOP(META.BACKUP_STOP, "PG_BACKUP_STOP"), //完成在线备份的执行
    SWITCH_WAL(META.SWITCH_WAL, "PG_SWITCH_WAL"), //强制服务器切换到新的预写日志文件
    WAL_FILE_NAME(META.WAL_FILE_NAME, "PG_WALFILE_NAME"), //将预写日志位置转换为保存该位置的WAL文件的名
    WAL_FILE_NAME_OFFSET(META.WAL_FILE_NAME_OFFSET, "PG_WALFILE_NAME_OFFSET"), //将预写日志位置转换为WAL文件名和该文件中的字节偏移量
    SPLIT_WAL_FILE_NAME(META.SPLIT_WAL_FILE_NAME, "PG_SPLIT_WALFILE_NAME"), //从 WAL 文件名中提取序列号和时间线 ID
    WAL_LSN_DIFF(META.WAL_LSN_DIFF, "PG_WAL_LSN_DIFF"), //计算两个预写日志位置之间的字节差
    IS_IN_RECOVERY(META.IS_IN_RECOVERY, "PG_IS_IN_RECOVERY"), //恢复是否在进行中
    LAST_WAL_RECEIVE_LSN(META.LAST_WAL_RECEIVE_LSN, "PG_LAST_WAL_RECEIVE_LSN"), //通过流复制接收并同步到磁盘的最后一个预写日志位置
    LAST_WAL_REPLAY_LSN(META.LAST_WAL_REPLAY_LSN, "PG_LAST_WAL_REPLAY_LSN"), //在恢复期间重放的最后一个预写日志位置
    LAST_XACT_REPLAY_TIMESTAMP(META.LAST_XACT_REPLAY_TIMESTAMP, "PG_LAST_XACT_REPLAY_TIMESTAMP"), //恢复期间重放的最后一个事务的时间戳
    GET_WAL_RESOURCE_MANAGERS(META.GET_WAL_RESOURCE_MANAGERS, "PG_GET_WAL_RESOURCE_MANAGERS"), //系统中当前加载的 WAL 资源管理器
    IS_WAL_REPLAY_PAUSED(META.IS_WAL_REPLAY_PAUSED, "PG_IS_WAL_REPLAY_PAUSED"), //请求恢复是否暂停
    GET_WAL_REPLAY_PAUSE_STATE(META.GET_WAL_REPLAY_PAUSE_STATE, "PG_GET_WAL_REPLAY_PAUSE_STATE"), //恢复暂停状态
    PROMOTE(META.PROMOTE, "PG_PROMOTE"), //将备用服务器提升为主状态
    WAL_REPLAY_PAUSE(META.WAL_REPLAY_PAUSE, "PG_WAL_REPLAY_PAUSE"), //请求暂停恢复
    WAL_REPLAY_RESUME(META.WAL_REPLAY_RESUME, "PG_WAL_REPLAY_RESUME"), //重新启动恢复
    EXPORT_SNAPSHOT(META.EXPORT_SNAPSHOT, "PG_EXPORT_SNAPSHOT"), //保存事务的当前快照并返回标识快照的字符串
    LOG_STANDBY_SNAPSHOT(META.LOG_STANDBY_SNAPSHOT, "PG_LOG_STANDBY_SNAPSHOT"), //拍摄正在运行的事务的快照并将其写入 WAL
    CREATE_PHYSICAL_REPLICATION_SLOT(META.CREATE_PHYSICAL_REPLICATION_SLOT, "PG_CREATE_PHYSICAL_REPLICATION_SLOT"), //创建新物理复制槽
    DROP_REPLICATION_SLOT(META.DROP_REPLICATION_SLOT, "PG_DROP_REPLICATION_SLOT"), //删除物理或逻辑复制槽
    CREATE_LOGICAL_REPLICATION_SLOT(META.CREATE_LOGICAL_REPLICATION_SLOT, "PG_CREATE_LOGICAL_REPLICATION_SLOT"), //使用输出插件插件创建新逻辑（解码）复制槽
    COPY_PHYSICAL_REPLICATION_SLOT(META.COPY_PHYSICAL_REPLICATION_SLOT, "PG_COPY_PHYSICAL_REPLICATION_SLOT"), //复制物理槽
    COPY_LOGICAL_REPLICATION_SLOT(META.COPY_LOGICAL_REPLICATION_SLOT, "PG_COPY_LOGICAL_REPLICATION_SLOT"), //复制逻辑槽
    LOGICAL_SLOT_GET_CHANGES(META.LOGICAL_SLOT_GET_CHANGES, "PG_LOGICAL_SLOT_GET_CHANGES"), //槽中的更改，从最后使用更改的点开始
    LOGICAL_SLOT_PEEK_CHANGES(META.LOGICAL_SLOT_PEEK_CHANGES, "PG_LOGICAL_SLOT_PEEK_CHANGES"), //
    LOGICAL_SLOT_GET_BINARY_CHANGES(META.LOGICAL_SLOT_GET_BINARY_CHANGES, "PG_LOGICAL_SLOT_GET_BINARY_CHANGES"), //
    LOGICAL_SLOT_PEEK_BINARY_CHANGES(META.LOGICAL_SLOT_PEEK_BINARY_CHANGES, "PG_LOGICAL_SLOT_PEEK_BINARY_CHANGES"), //
    REPLICATION_SLOT_ADVANCE(META.REPLICATION_SLOT_ADVANCE, "PG_REPLICATION_SLOT_ADVANCE"), //前进复制槽的当前确认位置
    REPLICATION_ORIGIN_CREATE(META.REPLICATION_ORIGIN_CREATE, "PG_REPLICATION_ORIGIN_CREATE"), //使用给定的外部名称创建复制源，并返回分配给它的内部 ID
    REPLICATION_ORIGIN_DROP(META.REPLICATION_ORIGIN_DROP, "PG_REPLICATION_ORIGIN_DROP"), //删除以前创建的复制源
    REPLICATION_ORIGIN_OID(META.REPLICATION_ORIGIN_OID, "PG_REPLICATION_ORIGIN_OID"), //按名称查找复制源并返回内部 ID
    REPLICATION_ORIGIN_SESSION_SETUP(META.REPLICATION_ORIGIN_SESSION_SETUP, "PG_REPLICATION_ORIGIN_SESSION_SETUP"), //将当前会话标记为从给定源的重播
    REPLICATION_ORIGIN_SESSION_RESET(META.REPLICATION_ORIGIN_SESSION_RESET, "PG_REPLICATION_ORIGIN_SESSION_RESET"), //取消效果
    REPLICATION_ORIGIN_SESSION_IS_SETUP(META.REPLICATION_ORIGIN_SESSION_IS_SETUP, "PG_REPLICATION_ORIGIN_SESSION_IS_SETUP"), //当前会话中选择了复制源
    REPLICATION_ORIGIN_SESSION_PROGRESS(META.REPLICATION_ORIGIN_SESSION_PROGRESS, "PG_REPLICATION_ORIGIN_SESSION_PROGRESS"), //当前会话中选择的复制源的重放位置
    REPLICATION_ORIGIN_XACT_SETUP(META.REPLICATION_ORIGIN_XACT_SETUP, "PG_REPLICATION_ORIGIN_XACT_SETUP"), //将当前事务标记为重播在给定 LSN 和时间戳处提交的事务
    REPLICATION_ORIGIN_XACT_RESET(META.REPLICATION_ORIGIN_XACT_RESET, "PG_REPLICATION_ORIGIN_XACT_RESET"), //取消效果
    REPLICATION_ORIGIN_ADVANCE(META.REPLICATION_ORIGIN_ADVANCE, "PG_REPLICATION_ORIGIN_ADVANCE"), //将给定节点的复制进度设置为给定位置
    REPLICATION_ORIGIN_PROGRESS(META.REPLICATION_ORIGIN_PROGRESS, "PG_REPLICATION_ORIGIN_PROGRESS"), //给定复制源的重放位置
    SYNC_REPLICATION_SLOTS(META.SYNC_REPLICATION_SLOTS, "PG_SYNC_REPLICATION_SLOTS"), //将逻辑故障转移复制槽从主服务器同步到备用服务器
    COLUMN_SIZE(META.COLUMN_SIZE, "PG_COLUMN_SIZE"), //显示用于存储任何单个数据值的字节数
    COLUMN_COMPRESSION(META.COLUMN_COMPRESSION, "PG_COLUMN_COMPRESSION"), //显示用于压缩单个可变长度值的压缩算法
    COLUMN_TOAST_CHUNK_ID(META.COLUMN_TOAST_CHUNK_ID, "PG_COLUMN_TOAST_CHUNK_ID"), //显示磁盘上的 TOASTED 值
    DATABASE_SIZE(META.DATABASE_SIZE, "PG_DATABASE_SIZE"), //计算具有指定名称或 OID 的数据库使用的总磁盘空间
    INDEXES_SIZE(META.INDEXES_SIZE, "PG_INDEXES_SIZE"), //计算附加到指定表的索引使用的总磁盘空间
    RELATION_SIZE(META.RELATION_SIZE, "PG_RELATION_SIZE"), //计算指定关系的一个“分支”使用的磁盘空间
    SIZE_BYTES(META.SIZE_BYTES, "PG_SIZE_BYTES"), //将可读格式的大小转换为字节
    SIZE_PRETTY(META.SIZE_PRETTY, "PG_SIZE_PRETTY"), //将以字节为单位的大小转换为更易于阅读的格式
    TABLE_SIZE(META.TABLE_SIZE, "PG_TABLE_SIZE"), //计算指定表使用的磁盘空间
    TABLESPACE_SIZE(META.TABLESPACE_SIZE, "PG_TABLESPACE_SIZE"), //计算具有指定名称或 OID 的表空间中使用的总磁盘空间
    TOTAL_RELATION_SIZE(META.TOTAL_RELATION_SIZE, "PG_TOTAL_RELATION_SIZE"), //计算指定表使用的总磁盘空间
    RELATION_FILE_NODE(META.RELATION_FILE_NODE, "PG_RELATION_FILENODE"), //当前分配给指定关系的“FILENODE”编号
    RELATION_FILEPATH(META.RELATION_FILEPATH, "PG_RELATION_FILEPATH"), //关系的整个文件路径名
    FILE_NODE_RELATION(META.FILE_NODE_RELATION, "PG_FILENODE_RELATION"), //给定表空间 OID 和存储它的文件节点
    COLLATION_ACTUAL_VERSION(META.COLLATION_ACTUAL_VERSION, "PG_COLLATION_ACTUAL_VERSION"), //排序规则对象当前安装在作系统中的实际版本
    DATABASE_COLLATION_ACTUAL_VERSION(META.DATABASE_COLLATION_ACTUAL_VERSION, "PG_DATABASE_COLLATION_ACTUAL_VERSION"), //当前安装在作系统中的数据库排序规则的实际版本
    IMPORT_SYSTEM_COLLATIONS(META.IMPORT_SYSTEM_COLLATIONS, "PG_IMPORT_SYSTEM_COLLATIONS"), //根据它在作系统中找到的所有区域设置将排序规则添加到系统目录中
    RESTORE_RELATION_STATS(META.RESTORE_RELATION_STATS, "PG_RESTORE_RELATION_STATS"), //更新表级统计信息
    CLEAR_RELATION_STATS(META.CLEAR_RELATION_STATS, "PG_CLEAR_RELATION_STATS"), //清除给定关系的表级统计信息
    RESTORE_ATTRIBUTE_STATS(META.RESTORE_ATTRIBUTE_STATS, "PG_RESTORE_ATTRIBUTE_STATS"), //创建或更新列级统计信息
    CLEAR_ATTRIBUTE_STATS(META.CLEAR_ATTRIBUTE_STATS, "PG_CLEAR_ATTRIBUTE_STATS"), //清除给定关系和属性的列级统计信息
    PARTITION_TREE(META.PARTITION_TREE, "PG_PARTITION_TREE"), //列出给定分区表或分区索引的分区树中的表或索引
    PARTITION_ANCESTORS(META.PARTITION_ANCESTORS, "PG_PARTITION_ANCESTORS"), //列出给定分区的祖先关系
    PARTITION_ROOT(META.PARTITION_ROOT, "PG_PARTITION_ROOT"), //给定关系所属的分区树的最顶层父级
    BRIN_SUMMARIZE_NEW_VALUES(META.BRIN_SUMMARIZE_NEW_VALUES, "BRIN_SUMMARIZE_NEW_VALUES"), //查找基表中索引当前未汇总的页面范围
    BRIN_SUMMARIZE_RANGE(META.BRIN_SUMMARIZE_RANGE, "BRIN_SUMMARIZE_RANGE"), //汇总涵盖给定块的页面范围
    BRIN_DE_SUMMARIZE_RANGE(META.BRIN_DE_SUMMARIZE_RANGE, "BRIN_DESUMMARIZE_RANGE"), //删除汇总覆盖给定表块的页面范围的 BRIN 索引元组。
    GIN_CLEAN_PENDING_LIST(META.GIN_CLEAN_PENDING_LIST, "GIN_CLEAN_PENDING_LIST"), //通过将指定 GIN 索引中的条目批量移动到主 GIN 数据结构来清理指定 GIN 索引的“待处理”列表
    LS_DIR(META.LS_DIR, "PG_LS_DIR"), //指定目录中所有文件
    LS_LOG_DIR(META.LS_LOG_DIR, "PG_LS_LOGDIR"), //服务器日志目录中每个普通文件的名称、大小和上次修改时间
    LS_WAL_DIR(META.LS_WAL_DIR, "PG_LS_WALDIR"), //服务器预写日志 （WAL） 目录中每个普通文件的名称、大小和上次修改时间
    LS_LOGICAL_MAP_DIR(META.LS_LOGICAL_MAP_DIR, "PG_LS_LOGICALMAPDIR"), //服务器目录中每个普通文件的名称、大小和上次修改时间
    LS_LOGICALS_NAP_DIR(META.LS_LOGICALS_NAP_DIR, "PG_LS_LOGICALSNAPDIR"), //服务器目录中每个普通文件的名称、大小和上次修改时间
    LS_REPLSLOT_DIR(META.LS_REPLSLOT_DIR, "PG_LS_REPLSLOTDIR"), //服务器目录中每个普通文件的名称、大小和上次修改时间
    LS_SUMMARIES_DIR(META.LS_SUMMARIES_DIR, "PG_LS_SUMMARIESDIR"), //服务器的WAL摘要目录中每个普通文件的名称，大小和上次修改时间
    LS_ARCHIVE_STATUS_DIR(META.LS_ARCHIVE_STATUS_DIR, "PG_LS_ARCHIVE_STATUSDIR"), //服务器的WAL存档状态目录中每个普通文件的名称，大小和上次修改时间
    LS_TMPDIR(META.LS_TMPDIR, "PG_LS_TMPDIR"), //指定表空间的临时文件目录中每个普通文件的名称、大小和上次修改时间
    READ_FILE(META.READ_FILE, "PG_READ_FILE"), //文本文件的全部或部分，从给定的字节偏移量开始，返回最长的字节
    READ_BINARY_FILE(META.READ_BINARY_FILE, "PG_READ_BINARY_FILE"), //全部或部分文件
    ADVISORY_LOCK_SHARED(META.ADVISORY_LOCK_SHARED, "PG_ADVISORY_LOCK_SHARED"), //获取共享会话级咨询锁
    ADVISORY_LOCK(META.ADVISORY_LOCK, "PG_ADVISORY_LOCK"), //获取独占会话级咨询锁
    ADVISORY_UNLOCK(META.ADVISORY_UNLOCK, "PG_ADVISORY_UNLOCK"), //释放以前获取的独占会话级咨询锁
    ADVISORY_UNLOCK_ALL(META.ADVISORY_UNLOCK_ALL, "PG_ADVISORY_UNLOCK_ALL"), //释放当前会话持有的所有会话级公告锁
    ADVISORY_UNLOCK_SHARED(META.ADVISORY_UNLOCK_SHARED, "PG_ADVISORY_UNLOCK_SHARED"), //释放先前获得的共享会话级咨询锁
    ADVISORY_XACT_LOCK(META.ADVISORY_XACT_LOCK, "PG_ADVISORY_XACT_LOCK"), //获得专属的交易级咨询锁
    ADVISORY_XACT_LOCK_SHARED(META.ADVISORY_XACT_LOCK_SHARED, "PG_ADVISORY_XACT_LOCK_SHARED"), //获得共享的交易级咨询锁
    TRY_ADVISORY_LOCK(META.TRY_ADVISORY_LOCK, "PG_TRY_ADVISORY_LOCK"), //获得独占的会话级咨询锁
    TRY_ADVISORY_LOCK_SHARED(META.TRY_ADVISORY_LOCK_SHARED, "PG_TRY_ADVISORY_LOCK_SHARED"), //共享会话级的咨询锁
    TRY_ADVISORY_XACT_LOCK(META.TRY_ADVISORY_XACT_LOCK, "PG_TRY_ADVISORY_XACT_LOCK"), //获得专属的交易级咨询锁
    TRY_ADVISORY_XACT_LOCK_SHARED(META.TRY_ADVISORY_XACT_LOCK_SHARED, "PG_TRY_ADVISORY_XACT_LOCK_SHARED"), //获取共享事务级咨询锁
    SUPPRESS_REDUNDANT_UPDATES_TRIGGER(META.SUPPRESS_REDUNDANT_UPDATES_TRIGGER, "SUPPRESS_REDUNDANT_UPDATES_TRIGGER"), //禁止执行不执行任何作的更新作
    TS_VECTOR_UPDATE_TRIGGER(META.TS_VECTOR_UPDATE_TRIGGER, "TSVECTOR_UPDATE_TRIGGER"), //自动更新关联的纯文本文档列中的列
    TS_VECTOR_UPDATE_TRIGGER_COLUMN(META.TS_VECTOR_UPDATE_TRIGGER_COLUMN, "TSVECTOR_UPDATE_TRIGGER_COLUMN"), //自动更新关联的纯文本文档列中的列
    EVENT_TRIGGER_DDL_COMMANDS(META.EVENT_TRIGGER_DDL_COMMANDS, "PG_EVENT_TRIGGER_DDL_COMMANDS"), //每个用户作执行的 DDL 命令列表
    EVENT_TRIGGER_TABLE_REWRITE_OID(META.EVENT_TRIGGER_TABLE_REWRITE_OID, "PG_EVENT_TRIGGER_TABLE_REWRITE_OID"), //即将重写的表的 OID
    EVENT_TRIGGER_TABLE_REWRITE_REASON(META.EVENT_TRIGGER_TABLE_REWRITE_REASON, "PG_EVENT_TRIGGER_TABLE_REWRITE_REASON"), //解释重写原因的代码
    MCV_LIST_ITEMS(META.MCV_LIST_ITEMS, "PG_MCV_LIST_ITEMS") //存储在多列 MCV 列表中的所有项目

    ;
    private final String name;
    private final META meta;
    private List<String> params = null;


    PostgresGenusFunction(META meta, String name) {
        this.meta = meta;
        this.name = name;
    }

    @Override
    public DatabaseType database() {
        return DatabaseType.MySQL;
    }

    @Override
    public META meta() {
        return meta;
    }

    @Override
    public List<String> params() {
        return params;
    }

    @Override
    public void params(List<String> params) {
        this.params = params;
    }

    @Override
    public String formula() {
        StringBuilder formula = new StringBuilder();
        formula.append(name).append("(");
        if(null != params) {
            formula.append(BeanUtil.concat(params, ","));
        }
        formula.append(")");
        return formula.toString();
    }
}
