/*
 * Copyright 2006-2025 www.anyline.org
 *
 * Licensed under the Apache License, Version 2.0 (the "License");
 * you may not use this file except in compliance with the License.
 * You may obtain a copy of the License at
 *
 *      http://www.apache.org/licenses/LICENSE-2.0
 *
 * Unless required by applicable law or agreed to in writing, software
 * distributed under the License is distributed on an "AS IS" BASIS,
 * WITHOUT WARRANTIES OR CONDITIONS OF ANY KIND, either express or implied.
 * See the License for the specific language governing permissions and
 * limitations under the License.
 */

package org.anyline.data.jdbc.adapter.init.function;

import org.anyline.metadata.SystemFunction;
import org.anyline.metadata.type.DatabaseType;
import org.anyline.util.BeanUtil;

import java.util.List;

public enum OracleGenusFunction implements SystemFunction {
    ABS(META.ABS, "ABS"),//
    ACOS(META.ACOS, "ACOS"),//
    ASIN(META.ASIN, "ASIN"),//
    ATAN(META.ATAN, "ATAN"),//
    ATAN2(META.ATAN2, "ATAN2"),//
    BIT_AND(META.BIT_AND, "BITAND"),//
    CEIL(META.CEIL, "CEIL"),//
    COS(META.COS, "COS"),//
    COSH(META.COSH, "COSH"),//
    EXP(META.EXP, "EXP"),//
    FLOOR(META.FLOOR, "FLOOR"),//
    LN(META.LN, "LN"),//
    LOG(META.LOG, "LOG"),//
    MOD(META.MOD, "MOD"),//
    NAN_VALUE(META.NAN_VALUE, "NANVL"),//
    POWER(META.POWER, "POWER"),//
    REMAINDER(META.REMAINDER, "REMAINDER"),//余数 与 MOD 函数不同
    ROUND(META.ROUND, "ROUND"),//
    SIGN(META.SIGN, "SIGN"),//
    SIN(META.SIN, "SIN"),//
    SINH(META.SINH, "SINH"),//
    SQRT(META.SQRT, "SQRT"),//
    TAN(META.TAN, "TAN"),//
    TANH(META.TANH, "TANH"),//
    TRUNC(META.TRUNC, "TRUNC"),//
    WIDTH_BUCKET(META.WIDTH_BUCKET, "WIDTH_BUCKET"),//
    CHR(META.CHR, "CHR"),//
    CONCAT(META.CONCAT, "CONCAT"),//
    INITCAP(META.INITCAP, "INITCAP"),//
    LOWER(META.LOWER, "LOWER"),//
    LPAD(META.LPAD, "LPAD"),//
    LTRIM(META.LTRIM, "LTRIM"),//
    NCHR(META.NCHR, "NCHR"),//
    NLS_INITCAP(META.NLS_INITCAP, "NLS_INITCAP"),//
    NLS_LOWER(META.NLS_LOWER, "NLS_LOWER"),//
    NLS_UPPER(META.NLS_UPPER, "NLS_UPPER"),//
    NLS_SORT(META.NLS_SORT, "NLSSORT"),//
    REGEXP_REPLACE(META.REGEXP_REPLACE, "REGEXP_REPLACE"),//
    REGEXP_SUBSTR(META.REGEXP_SUBSTR, "REGEXP_SUBSTR"),//
    REPLACE(META.REPLACE, "REPLACE"),//
    RPAD(META.RPAD, "RPAD"),//
    RTRIM(META.RTRIM, "RTRIM"),//
    SOUNDEX(META.SOUNDEX, "SOUNDEX"),//
    SUBSTR(META.SUBSTR, "SUBSTR"),//
    TRANSLATE(META.TRANSLATE, "TRANSLATE"),//
    TRIM(META.TRIM, "TRIM"),//
    UPPER(META.UPPER, "UPPER"),//
    ASCII(META.ASCII, "ASCII"),//
    INSTR(META.INSTR, "INSTR"),//
    LENGTH(META.LENGTH, "LENGTH"),//
    REGEXP_COUNT(META.REGEXP_COUNT, "REGEXP_COUNT"),//
    REGEXP_INSTR(META.REGEXP_INSTR, "REGEXP_INSTR"),//
    NLS_CHARSET_DECL_LEN(META.NLS_CHARSET_DECL_LEN, "NLS_CHARSET_DECL_LEN"),//
    NLS_CHARSET_ID(META.NLS_CHARSET_ID, "NLS_CHARSET_ID"),//
    NLS_CHARSET_NAME(META.NLS_CHARSET_NAME, "NLS_CHARSET_NAME"),//
    COLLATION(META.COLLATION, "COLLATION"),//
    NLS_COLLATION_ID(META.NLS_COLLATION_ID, "NLS_COLLATION_ID"),//
    NLS_COLLATION_NAME(META.NLS_COLLATION_NAME, "NLS_COLLATION_NAME"),//
    ADD_MONTHS(META.ADD_MONTHS, "ADD_MONTHS"),//
    CURRENT_DATE(META.CURRENT_DATE, "CURRENT_DATE"),//
    CURRENT_TIMESTAMP(META.CURRENT_TIMESTAMP, "CURRENT_TIMESTAMP"),//
    TIME_ZONE(META.TIME_ZONE, "DBTIMEZONE"),//
    EXTRACT(META.EXTRACT, "EXTRACT"),//
    FROM_TZ(META.FROM_TZ, "FROM_TZ"),//
    LAST_DAY(META.LAST_DAY, "LAST_DAY"),//
    LOCALTIMESTAMP(META.LOCAL_TIMESTAMP, "LOCALTIMESTAMP"),//
    MONTHS_BETWEEN(META.MONTHS_BETWEEN, "MONTHS_BETWEEN"),//
    NEW_TIME(META.NEW_TIME, "NEW_TIME"),//
    NEXT_DAY(META.NEXT_DAY, "NEXT_DAY"),//
    NUMBER_TO_DS_INTERVAL(META.NUMBER_TO_DS_INTERVAL, "NUMTODSINTERVAL"),//
    NUMBER_TO_YM_INTERVAL(META.NUMBER_TO_YM_INTERVAL, "NUMTOYMINTERVAL"),//
    DST_AFFECTED(META.DST_AFFECTED, "ORA_DST_AFFECTED"),//解析为一个带时区的TIMESTAMP值
    DST_CONVERT(META.DST_CONVERT, "ORA_DST_CONVERT"),//指定的日期时间表达式指定错误处理方式
    ORA_DST_ERROR(META.DST_ERROR, "ORA_DST_ERROR"),//解析为一个 TIMESTAMP WITH TIME ZONE 值 并指示新的日期时间值是否会导致时区数据出错
    SESSIONTIMEZONE(META.SESSION_TIMEZONE, "SESSIONTIMEZONE"),//
    SYS_EXTRACT_UTC(META.SYS_EXTRACT_UTC, "SYS_EXTRACT_UTC"),//从带有时区偏移或时区区域名称的日期时间值中提取UTC
    SYS_DATE(META.SYS_DATE, "SYSDATE"),//
    SYS_TIMESTAMP(META.CURRENT_TIMESTAMP, "SYSTIMESTAMP"),//
    TO_CHAR(META.TO_CHAR, "TO_CHAR"),//
    TO_DAY_SECOND_INTERVAL(META.TO_DAY_SECOND_INTERVAL, "TO_DSINTERVAL"),//换为 INTERVAL DAY TO SECOND 数据类型的值
    TO_TIMESTAMP(META.TO_TIMESTAMP, "TO_TIMESTAMP"),//
    TO_TIMESTAMP_TZ(META.TO_TIMESTAMP_TZ, "TO_TIMESTAMP_TZ"),//
    TO_YMINTERVAL(META.TO_YEAR_MONTH_INTERVAL, "TO_YMINTERVAL"),//转换为 INTERVAL MONTH TO YEAR 数据类型的值
    TZ_OFFSET(META.TIME_ZONE_OFFSET, "TZ_OFFSET"),//与参数对应的时区偏移量
    GREATEST(META.GREATEST, "GREATEST"),//
    LEAST(META.LEAST, "LEAST"),//
    TO_ASCII(META.ASCII , "ASCIISTR"),//
    BIN_TO_NUM(META.BIN_TO_NUM, "BIN_TO_NUM"),//二进制位转换为十进制数
    CAST(META.CAST, "CAST"),//
    CHAR_TO_ROWID(META.CHAR_TO_ROWID, "CHARTOROWID"),//字符串值转换为 ROWID 数据类型
    COMPOSE(META.COMPOSE, "COMPOSE"),//将 Unicode 规范组合应用于给定的字符串参数的结果
    CONVERT(META.CONVERT, "CONVERT"),//
    DECOMPOSE(META.DECOMPOSE, "DECOMPOSE"),//
    HEX_TO_RAW(META.HEX_TO_RAW, "HEXTORAW"),//
    RAW_TO_HEX(META.RAW_TO_HEX, "RAWTOHEX"),//
    RAW_TO_NHEX(META.RAW_TO_NHEX, "RAWTONHEX"),//
    ROW_ID_TO_CHAR(META.ROW_ID_TO_CHAR, "ROWIDTOCHAR"),//
    ROW_ID_TO_NCHAR(META.ROW_ID_TO_NCHAR, "ROWIDTONCHAR"),//
    SCN_TO_TIMESTAMP(META.SCN_TO_TIMESTAMP, "SCN_TO_TIMESTAMP"),//给定的系统更改号（SCN）相关联的近似时间戳
    TIMESTAMP_TO_SCN(META.TIMESTAMP_TO_SCN, "TIMESTAMP_TO_SCN"),//
    TO_BINARY_DOUBLE(META.TO_BINARY_DOUBLE, "TO_BINARY_DOUBLE"),//
    TO_BINARY_FLOAT(META.TO_BINARY_FLOAT, "TO_BINARY_FLOAT"),//
    TO_BLOB(META.TO_BLOB, "TO_BLOB"),//
    TO_DATE(META.TO_DATE, "TO_DATE"),//
    TO_LOB(META.TO_LOB, "TO_LOB"),//将列给定的列中的 LONG 或 LONG RAW 值转换为 LOB 值
    TO_MULTI_BYTE(META.TO_MULTI_BYTE, "TO_MULTI_BYTE"),//
    TO_NCHAR(META.TO_NCHAR, "TO_NCHAR"),//
    TO_NCLOB(META.TO_NCLOB, "TO_NCLOB"),//
    TO_NUMBER(META.TO_NUMBER, "TO_NUMBER"),//
    TO_SINGLE_BYTE(META.TO_SINGLE_BYTE, "TO_SINGLE_BYTE"),//
    TREAT(META.TREAT, "TREAT"),//
    UNI_STR(META.UNI_STR, "UNISTR"),//以国家字符集返回给定的字符数据
    VALIDATE_CONVERSION(META.VALIDATE_CONVERSION, "VALIDATE_CONVERSION"),//
    BFILE_NAME(META.BFILE_NAME, "BFILENAME"),//
    CARDINALITY(META.CARDINALITY, "CARDINALITY"),//
    COLLECT(META.COLLECT, "COLLECT"),//置于 CAST 函数内部使用
    POWER_MULTIS_ET(META.POWER_MULTIS_ET, "POWERMULTISET"),//一个包含输入嵌套表所有非空子集（称为子多重集）的嵌套表
    POWER_MULTIS_ET_BY_CARDINALITY(META.POWER_MULTIS_ET_BY_CARDINALITY, "POWERMULTISET_BY_CARDINALITY"),//接收一个嵌套表和一个基数作为输入，返回一个包含所有非空子集（称为子多重集）的嵌套表嵌套表
    SET(META.SET, "SET"),//通过消除重复项将嵌套表转换为集合
    SYS_CONNECT_BY_PATH(META.SYS_CONNECT_BY_PATH, "SYS_CONNECT_BY_PATH"),//列值从根到节点的路径
    CLUSTER_DISTANCE(META.CLUSTER_DISTANCE, "CLUSTER_DISTANCE"),//所选内容中每一行的簇距离
    CLUSTER_ID(META.CLUSTER_ID, "CLUSTER_ID"),//选择中每一行的最高概率簇的标识符
    CLUSTER_PROBABILITY(META.CLUSTER_PROBABILITY, "CLUSTER_PROBABILITY"),//选择中每一行的概率
    CLUSTER_SET(META.CLUSTER_SET, "CLUSTER_SET"),//返回每行的簇ID和概率对
    FEATURE_COMPARE(META.FEATURE_COMPARE, "FEATURE_COMPARE"),//使用特征提取模型来比较两个不同的文档
    FEATURE_DETAILS(META.FEATURE_DETAILS, "FEATURE_DETAILS"),//选择中每一行的返回功能细节
    FEATURE_ID(META.FEATURE_ID, "FEATURE_ID"),//选择中每一行最高值特征的标识符
    FEATURE_SET(META.FEATURE_SET, "FEATURE_SET"),//所选中每一行的一组要素 ID 和要素值对
    FEATURE_VALUE(META.FEATURE_VALUE, "FEATURE_VALUE"),//所选中每一行的要素值
    DM_PARTITION_NAME(META.DM_PARTITION_NAME, "ORA_DM_PARTITION_NAME"),//与输入行关联的分区名称
    PREDICTION(META.PREDICTION, "PREDICTION"),//所选内容中每一行的预测
    PREDICTION_BOUNDS(META.PREDICTION_BOUNDS, "PREDICTION_BOUNDS"),//预测所选内容中每一行的类或值
    PREDICTION_COST(META.PREDICTION_COST, "PREDICTION_COST"),//选择中每一行的成本
    PREDICTION_DETAILS(META.PREDICTION_DETAILS, "PREDICTION_DETAILS"),//选择中每一行的预测细节
    PREDICTION_PROBABILITY(META.PREDICTION_PROBABILITY, "PREDICTION_PROBABILITY"),//选择中每一行的概率
    PREDICTION_SET(META.PREDICTION_SET, "PREDICTION_SET"),//所选内容中每一行的概率或成本
    DEPTH(META.DEPTH, "DEPTH"),//条件指定的路径中具有相同相关变量的级别数
    EXISTS_NODE(META.EXISTS_NODE, "EXISTSNODE"),//通过指定路径遍历XML文档是否会得到任何节点
    EXTRACT_VALUE(META.EXTRACT_VALUE, "EXTRACTVALUE"),//结果节点的标量值
    PATH(META.PATH, "PATH"),//指向父条件中指定的资源的相对路径
    SYS_DB_URI_GEN(META.SYS_DB_URI_GEN, "SYS_DBURIGEN"),//生成特定列或行对象的数据类型URL
    SYS_XML_AGG(META.SYS_XML_AGG, "SYS_XMLAGG"),//聚合 expr 表示的所有 XML 文档或片段，并生成单个 XML 文档
    SYS_XML_GEN(META.SYS_XML_GEN, "SYS_XMLGEN"),//包含 XML 文档的类型的实例
    XMLAGG(META.XML_AGG, "XMLAGG"),//
    XML_CAST(META.XML_CAST, "XMLCAST"),//转换为 datatype 指定的标量 SQL 数据类型
    XML_CDATA(META.XML_CDATA, "XMLCDATA"),//
    XML_COL_ATT_VAL(META.XML_COL_ATT_VAL, "XMLCOLATTVAL"),//
    XML_COMMENT(META.XML_COMMENT, "XMLCOMMENT"),//
    XMLCONCAT(META.XML_CONCAT, "XMLCONCAT"),//
    XML_DIFF(META.XML_DIFF, "XMLDIFF"),//
    XML_ELEMENT(META.XML_ELEMENT, "XMLELEMENT"),//
    XML_EXISTS(META.XML_EXISTS, "XMLEXISTS"),//
    XML_FOREST(META.XML_FOREST, "XMLFOREST"),//
    XML_IS_VALID(META.XML_IS_VALID, "XMLISVALID"),//是否符合相关的XML模式
    XML_PARSE(META.XML_PARSE, "XMLPARSE"),//解析并从评估结果生成 XML 实例
    XML_PATCH(META.XML_PATCH, "XMLPATCH"),//更改修补 XML 文档
    XML_PI(META.XML_PI, "XMLPI"),//
    XML_QUERY(META.XML_QUERY, "XMLQUERY"),//计算 XQuery 表达式的结果
    XML_ROOT(META.XML_ROOT, "XMLROOT"),//
    XML_SEQUENCE(META.XML_SEQUENCE, "XMLSEQUENCE"),//
    XML_SERIALIZE(META.XML_SERIALIZE, "XMLSERIALIZE"),//
    XML_TABLE(META.XML_TABLE, "XMLTABLE"),//
    XML_TRANSFORM(META.XML_TRANSFORM, "XMLTRANSFORM"),//
    JSON_QUERY(META.JSON_QUERY, "JSON_QUERY"),//
    JSON_TABLE(META.JSON_TABLE, "JSON_TABLE"),//
    JSON_VALUE(META.JSON_VALUE, "JSON_VALUE"),//
    JSON_ARRAY(META.JSON_ARRAY, "JSON_ARRAY"),//
    JSON_ARRAY_AGG(META.JSON_ARRAY_AGG, "JSON_ARRAYAGG"),//
    JSON_OBJECT(META.JSON_OBJECT, "JSON_OBJECT"),//
    JSON_OBJECT_AGG(META.JSON_OBJECT_AGG, "JSON_OBJECTAGG"),//
    JSON_DATA_GUIDE(META.JSON_DATA_GUIDE, "JSON_DATAGUIDE"),//包含该 JSON 文档平面数据指南的值
    DECODE(META.DECODE, "DECODE"),//
    DUMP(META.DUMP, "DUMP"),//包含数据类型代码、字节长度和expr内部表示的值
    HASH(META.HASH, "ORA_HASH"),//计算给定表达式哈希值
    STANDARD_HASH(META.STANDARD_HASH, "STANDARD_HASH"),//计算给定表达式的哈希值
    VSIZE(META.VSIZE, "VSIZE"),//内部表示中的字节数
    COALESCE(META.COALESCE, "COALESCE"),//
    LNNVL(META.LNNVL, "LNNVL"),//条件取反
    NANVL(META.NANVL, "NANVL"),//当第一个参数为NaN（非数字）时返回第二个参数
    NULLIF(META.NULLIF, "NULLIF"),//
    NVL(META.NVL, "NVL"),//
    NVL2(META.NVL2, "NVL2"),//
    CONVERT_DBID_TO_ID(META.CONVERT_DBID_TO_ID, "CON_DBID_TO_ID"),//将容器 DBID 作为其参数并返回容器 ID
    CONVERT_GUID_TO_ID(META.CONVERT_GUID_TO_ID, "CON_GUID_TO_ID"),//
    CONVERT_NAME_TO_ID(META.CONVERT_NAME_TO_ID, "CON_NAME_TO_ID"),//
    CONVERT_UID_TO_ID(META.CONVERT_UID_TO_ID, "CON_UID_TO_ID"),//
    ORA_INVOKING_USER(META.INVOKING_USER, "ORA_INVOKING_USER"),//
    ORA_INVOKING_USERID(META.INVOKING_USER_ID, "ORA_INVOKING_USERID"),//
    SYS_CONTEXT(META.SYS_CONTEXT, "SYS_CONTEXT"),//
    SYS_GUID(META.SYS_GUID, "SYS_GUID"),//
    SYS_TYPEID(META.SYS_TYPE_ID, "SYS_TYPEID"),//最具体类型的类型
    UID(META.CURRENT_USER_ID, "UID"),//用于唯一标识会话用户
    USER(META.USER, "USER"),//
    USERENV(META.USER_ENV, "USERENV"),//当前会话的信息
    APPROX_COUNT(META.APPROX_COUNT, "APPROX_COUNT"),//表达式的近似计数
    APPROX_COUNT_DISTINCT(META.APPROX_COUNT_DISTINCT, "APPROX_COUNT_DISTINCT"),//包含不同 expr 值的行数
    APPROX_COUNT_DISTINCT_AGG(META.APPROX_COUNT_DISTINCT_AGG, "APPROX_COUNT_DISTINCT_AGG"),//将包含有关近似非重复值计数的信息的详细信息列作为其输入，并使您能够执行这些计数的聚合
    APPROX_COUNT_DISTINCT_DETAIL(META.APPROX_COUNT_DISTINCT_DETAIL, "APPROX_COUNT_DISTINCT_DETAIL"),//计算包含不同expr值的行数的近似信息，并返回一个称为detail的BLOB值，该值以特殊格式包含该信息
    APPROX_MEDIAN(META.APPROX_MEDIAN, "APPROX_MEDIAN"),//一个近似的中间值或一个近似插值
    APPROX_PERCENTILE(META.APPROX_PERCENTILE, "APPROX_PERCENTILE"),//百分位数值会落入该百分比值
    APPROX_PERCENTILE_AGG(META.APPROX_PERCENTILE_AGG, "APPROX_PERCENTILE_AGG"),//
    APPROX_PERCENTILE_DETAIL(META.APPROX_PERCENTILE_DETAIL, "APPROX_PERCENTILE_DETAIL"),//计算 expr 值的近似百分位数信息
    APPROX_RANK(META.APPROX_RANK, "APPROX_RANK"),//一组值中的近似值
    APPROX_SUM(META.APPROX_SUM, "APPROX_SUM"),//表达式的近似和
    AVG(META.AVG, "AVG"),//
    CORR(META.CORR, "CORR"),//
    CORR_S(META.CORR_S, "CORR_S"),//计算斯皮尔曼的ρ相关系数
    CORR_K(META.CORR_K, "CORR_K"),//计算肯德尔的 tau-b 相关系数
    COUNT(META.COUNT, "COUNT"),//
    COVAR_POP(META.COVAR_POP, "COVAR_POP"),//
    COVAR_SAMP(META.COVAR_SAMP, "COVAR_SAMP"),//
    CUME_DIST(META.CUME_DIST, "CUME_DIST"),//
    DENSE_RANK(META.DENSE_RANK, "DENSE_RANK"),//
    FIRST(META.FIRST_ROW, "FIRST"),//
    GROUP_ID(META.GROUP_ID, "GROUP_ID"),//
    GROUPING(META.GROUPING, "GROUPING"),//
    GROUPING_ID(META.GROUPING_ID, "GROUPING_ID"),//
    LAST(META.LAST_ROW, "LAST"),//
    LIST_AGG(META.LIST_AGG, "LISTAGG"),//
    MAX(META.MAX, "MAX"),//
    MEDIAN(META.MEDIAN, "MEDIAN"),//
    MIN(META.MIN, "MIN"),//
    PERCENT_RANK(META.PERCENT_RANK, "PERCENT_RANK"),//
    PERCENTILE_CONT(META.PERCENTILE_CONT, "PERCENTILE_CONT"),//
    PERCENTILE_DISC(META.PERCENTILE_DISC, "PERCENTILE_DISC"),//
    RANK(META.RANK, "RANK"),//
    STATS_BINOMIAL_TEST(META.STATS_BINOMIAL_TEST, "STATS_BINOMIAL_TEST"),//
    STATS_CROSSTAB(META.STATS_CROSS_TAB, "STATS_CROSSTAB"),//
    STATS_F_TEST(META.STATS_F_TEST, "STATS_F_TEST"),//
    STATS_KS_TEST(META.STATS_KS_TEST, "STATS_KS_TEST"),//
    STATS_MODE(META.STATS_MODE, "STATS_MODE"),//
    STATS_MW_TEST(META.STATS_MW_TEST, "STATS_MW_TEST"),//
    STATS_ONE_WAY_ANOVA(META.STATS_ONE_WAY_ANOVA, "STATS_ONE_WAY_ANOVA"),//
    STATS_T_TEST_ONE(META.STATS_T_TEST_ONE, "STATS_T_TEST_ONE"),//
    STATS_T_TEST_PAIRED(META.STATS_T_TEST_PAIRED, "STATS_T_TEST_PAIRED"),//
    STATS_T_TEST_INDEP(META.STATS_T_TEST_INDEP, "STATS_T_TEST_INDEP"),//
    STATS_T_TEST_INDEPU(META.STATS_T_TEST_INDEPU, "STATS_T_TEST_INDEPU"),//
    STATS_WSR_TEST(META.STATS_WSR_TEST, "STATS_WSR_TEST"),//
    STDDEV(META.STDDEV, "STDDEV"),//
    STDDEV_POP(META.STDDEV_POP, "STDDEV_POP"),//
    STDDEV_SAMP(META.STDDEV_SAMP, "STDDEV_SAMP"),//
    SUM(META.SUM, "SUM"),//
    SYS_OP_ZONE_ID(META.SYS_OP_ZONE_ID, "SYS_OP_ZONE_ID"),//将 rowid 作为其参数并返回区域 ID
    TO_APPROX_COUNT_DISTINCT(META.TO_APPROX_COUNT_DISTINCT, "TO_APPROX_COUNT_DISTINCT"),//
    TO_APPROX_PERCENTILE(META.TO_APPROX_PERCENTILE, "TO_APPROX_PERCENTILE"),//
    VAR_POP(META.VAR_POP, "VAR_POP"),//
    VAR_SAMP(META.VAR_SAMP, "VAR_SAMP"),//
    VARIANCE(META.VARIANCE, "VARIANCE"),//
    CLUSTER_DETAILS(META.CLUSTER_DETAILS, "CLUSTER_DETAILS"),//所选内容中每一行的集群详细信息
    FIRST_VALUE(META.FIRST_VALUE, "FIRST_VALUE"),//
    LAG(META.LAG, "LAG"),//
    LAST_VALUE(META.LAST_VALUE, "LAST_VALUE"),//
    LEAD(META.LEAD, "LEAD"),//
    NTH_VALUE(META.NTH_VALUE, "NTH_VALUE"),//
    NTILE(META.NTILE, "NTILE"),//
    RATIO_TO_REPORT(META.RATIO_TO_REPORT, "RATIO_TO_REPORT"),//计算一个值与一组值之和的比值
    ROW_NUMBER(META.ROW_NUMBER, "ROW_NUMBER"),//
    DEREF(META.DEREF, "DEREF"),//参数expr的对象引用
    MAKE_REF(META.MAKE_REF, "MAKE_REF"),//创建对象视图的行或对象表中的行，其对象标识符基于主键
    REF(META.REF, "REF"),//为绑定到变量或行的对象实例返回一个值
    REFTOHEX(META.REF_TO_HEX, "REFTOHEX"),//将参数expr转换为包含其十六进制等价项的字符值
    CV(META.CV, "CV"),//从规则的左侧到右侧的维度列或分区列的当前值
    ITERATION_NUMBER(META.ITERATION_NUMBER, "ITERATION_NUMBER"),//表示通过模型规则完成的迭代
    PRESENTNNV(META.PRESENTNNV, "PRESENTNNV"),//
    PRESENTV(META.PRESENTV, "PRESENTV"),//
    PREVIOUS(META.PREVIOUS, "PREVIOUS"),//
    CUBE_TABLE(META.CUBE_TABLE, "CUBE_TABLE"),//
    DATA_OBJECT_TO_MAT_PARTITION(META.DATA_OBJECT_TO_MAT_PARTITION, "DATAOBJ_TO_MAT_PARTITION"),//
    DATA_OBJECT_TO_PARTITION(META.DATA_OBJECT_TO_PARTITION, "DATAOBJ_TO_PARTITION"),//域索引数据的存储位置

    ;
    private final String name;
    private final META meta;
    private List<String> params = null;


    OracleGenusFunction(META meta, String name) {
        this.meta = meta;
        this.name = name;
    }

    @Override
    public DatabaseType database() {
        return DatabaseType.MySQL;
    }

    @Override
    public META meta() {
        return meta;
    }

    @Override
    public List<String> params() {
        return params;
    }

    @Override
    public void params(List<String> params) {
        this.params = params;
    }

    @Override
    public String formula() {
        StringBuilder formula = new StringBuilder();
        formula.append(name).append("(");
        if(null != params) {
            formula.append(BeanUtil.concat(params, ","));
        }
        formula.append(")");
        return formula.toString();
    }
}
